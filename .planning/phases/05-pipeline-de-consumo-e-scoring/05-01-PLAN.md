---
phase: 05-pipeline-de-consumo-e-scoring
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_scoring.py
  - tests/test_consumo.py
autonomous: true
requirements:
  - SCR-01
  - SCR-02
  - SCR-03
  - SCR-04
  - SCR-05
  - CONS-01
  - CONS-02
  - CONS-03
  - TEST-01
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "tests/test_scoring.py existe com testes dos 5 fatores e composicao final"
    - "tests/test_consumo.py existe com teste de integracao end-to-end usando ChromaDB real"
    - "Todos os testes falham com ImportError ou AssertionError antes da implementacao"
    - "Nenhum teste existente (test_agentes.py, test_repositorio.py, etc.) e quebrado por estes arquivos"
  artifacts:
    - path: "tests/test_scoring.py"
      provides: "Testes RED dos 5 fatores de scoring e composicao ponderada"
      exports: []
    - path: "tests/test_consumo.py"
      provides: "Testes RED do pipeline de consumo end-to-end"
      exports: []
  key_links:
    - from: "tests/test_scoring.py"
      to: "connect_ai/scoring.py"
      via: "from connect_ai.scoring import ..."
      pattern: "from connect_ai\\.scoring import"
    - from: "tests/test_consumo.py"
      to: "connect_ai/agentes.py"
      via: "from connect_ai.agentes import buscar_matches"
      pattern: "from connect_ai\\.agentes import buscar_matches"
---

<objective>
Criar os testes TDD RED para o modulo de scoring e para o pipeline de consumo end-to-end.

Proposito: Estabelecer os contratos verificaveis de toda a Fase 5 antes de qualquer implementacao.
O commit RED define o que "pronto" significa — os testes governam a implementacao, nao o contrario.

Output: tests/test_scoring.py e tests/test_consumo.py com todos os testes falhando (ImportError
para connect_ai.scoring, que ainda nao existe, e AssertionError para integracoes nao implementadas).
</objective>

<execution_context>
@C:/Users/power/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/power/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@connect_ai/schema.py
@connect_ai/agentes.py
@connect_ai/repositorio.py
@connect_ai/ingestao.py
@tests/test_repositorio.py
@tests/test_agentes.py

<interfaces>
<!-- Interfaces que os testes RED devem referenciar (ainda nao existem — causarao ImportError) -->

<!-- De connect_ai/scoring.py (A SER CRIADO na Wave 2): -->
```python
# Funcoes esperadas:
def score_semantico(distancia_coseno: float) -> float:
    """distancia_coseno em [0,2] -> score em [0,100]
    Formula: (1 - distancia/2) * 100
    """

def score_interesses(interesses_a: list[str], interesses_b: list[str]) -> float:
    """Interesses em comum -> score em [0,20]
    Formula: min(len(set(a) & set(b)) * 5, 20)
    """

def score_objetivo(objetivo_a: str, objetivo_b: str) -> float:
    """100.0 se objetivo_a == objetivo_b, 0.0 caso contrario"""

def score_idade(idade_a: int, idade_b: int) -> float:
    """Formula: max(0.0, min(100.0, 100 - abs(idade_a - idade_b) * 2))"""

def score_geografia(cidade_a: str, cidade_b: str) -> float:
    """100.0 se cidade_a == cidade_b, 0.0 caso contrario"""

def calcular_score(
    distancia_coseno: float,
    interesses_a: list[str],
    interesses_b: list[str],
    objetivo_a: str,
    objetivo_b: str,
    idade_a: int,
    idade_b: int,
    cidade_a: str,
    cidade_b: str,
) -> dict:
    """Retorna dict com score_final e breakdown dos 5 fatores.
    score_final = semantico*0.60 + interesses*0.20 + objetivo*0.10 + idade*0.05 + geografia*0.05
    Retorno: {
        "score_final": float,
        "score_semantico": float,
        "score_interesses": float,
        "score_objetivo": float,
        "score_idade": float,
        "score_geografia": float,
    }
    """
```

<!-- De connect_ai/agentes.py (PIPELINE_CONSUMO ja existe, buscar_matches A SER CRIADO): -->
```python
# pipeline_consumo: ja compilado no agentes.py (Fase 3)
# buscar_matches: funcao nova a ser criada na Fase 5
def buscar_matches(perfil_solicitante: Perfil, colecao: Repositorio) -> list[dict]:
    """Orquestra pipeline end-to-end: filtros hard -> busca vetorial Top-30
    -> scoring ponderado -> corte >= 85 -> Top-10.
    Cada dict no retorno tem: id, score, nome, cidade, idade,
    score_semantico, score_interesses, score_objetivo, score_idade, score_geografia.
    """
```

<!-- De connect_ai/seed_data.py (JA EXISTE): -->
```python
PERFIL_TESTE  # Perfil com nome="Ana Lima", objetivo="namoro", idade=27, genero="feminino", genero_preferido="masculino"
gerar_pool_perfis()  # -> List[Perfil] com 100 perfis (20 alta-compat + 80 diversidade)
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Tarefa 1: Testes RED para connect_ai/scoring.py (TEST-01, SCR-01..SCR-05)</name>
  <files>tests/test_scoring.py</files>

  <read_first>
    - connect_ai/schema.py (estrutura de Perfil, campos interesses/objetivo/idade/cidade)
    - tests/test_repositorio.py (padrao de imports lazy, estrutura de fixtures do projeto)
    - tests/test_agentes.py (padrao de imports lazy dentro de funcoes, convencao de nomes PT-BR)
  </read_first>

  <behavior>
    <!-- RED: todos falham com ImportError pois connect_ai/scoring.py nao existe -->

    FATOR 1 — score_semantico:
    - distancia=0.0 -> 100.0 (perfil identico)
    - distancia=1.0 -> 50.0 (perfil ortogonal)
    - distancia=2.0 -> 0.0 (perfil oposto)
    - distancia=0.5 -> 75.0

    FATOR 2 — score_interesses:
    - 0 interesses em comum -> 0.0
    - 1 interesse em comum -> 5.0
    - 4 interesses em comum -> 20.0 (max)
    - 5 interesses em comum -> 20.0 (truncado, nao 25)

    FATOR 3 — score_objetivo:
    - mesmos objetivos -> 100.0
    - objetivos diferentes -> 0.0

    FATOR 4 — score_idade:
    - mesma idade -> 100.0
    - diferenca de 5 anos -> 90.0
    - diferenca de 50 anos -> 0.0 (truncado, nao negativo)
    - diferenca de 49 anos -> 2.0

    FATOR 5 — score_geografia:
    - mesma cidade -> 100.0
    - cidades diferentes -> 0.0

    COMPOSICAO — calcular_score:
    - Retorna dict com chaves: score_final, score_semantico, score_interesses, score_objetivo, score_idade, score_geografia
    - score_final = semantico*0.60 + interesses*0.20 + objetivo*0.10 + idade*0.05 + geografia*0.05
    - Caso perfeito: dist=0, 4 interesses iguais, mesmo objetivo, mesma idade, mesma cidade -> score_final == 100*0.60 + 20*0.20 + 100*0.10 + 100*0.05 + 100*0.05 = 60+4+10+5+5 = 84.0
    - Caso perfeito com mesmo perfil (dist=0, interesses_a==interesses_b com 4+): deve ser >= 85
    - score_final em [0.0, 100.0]
  </behavior>

  <action>
Criar tests/test_scoring.py com os seguintes requisitos:

1. IMPORTS LAZY dentro de cada funcao de teste (padrao do projeto — ver test_agentes.py).
   Usar: `from connect_ai.scoring import score_semantico` DENTRO da funcao, nao no topo.
   Isso garante que cada teste falhe individualmente com ImportError em RED.

2. Docstring do modulo em PT-BR explicando que cobre SCR-01..SCR-05 e TEST-01.

3. Funcoes de teste (sem fixtures necessarias — testes puros com valores literais):

```python
def test_score_semantico_distancia_zero():
    from connect_ai.scoring import score_semantico
    assert score_semantico(0.0) == pytest.approx(100.0)

def test_score_semantico_distancia_um():
    from connect_ai.scoring import score_semantico
    assert score_semantico(1.0) == pytest.approx(50.0)

def test_score_semantico_distancia_dois():
    from connect_ai.scoring import score_semantico
    assert score_semantico(2.0) == pytest.approx(0.0)

def test_score_semantico_distancia_meio():
    from connect_ai.scoring import score_semantico
    assert score_semantico(0.5) == pytest.approx(75.0)

def test_score_interesses_zero_comum():
    from connect_ai.scoring import score_interesses
    assert score_interesses(["musica"], ["viagem"]) == pytest.approx(0.0)

def test_score_interesses_um_comum():
    from connect_ai.scoring import score_interesses
    assert score_interesses(["musica", "viagem"], ["musica", "leitura"]) == pytest.approx(5.0)

def test_score_interesses_quatro_comum():
    from connect_ai.scoring import score_interesses
    a = ["musica", "viagem", "leitura", "cinema"]
    b = ["musica", "viagem", "leitura", "cinema", "esportes"]
    assert score_interesses(a, b) == pytest.approx(20.0)

def test_score_interesses_truncado_em_vinte():
    from connect_ai.scoring import score_interesses
    # 5 em comum -> min(5*5, 20) = 20, nao 25
    a = ["a", "b", "c", "d", "e"]
    b = ["a", "b", "c", "d", "e", "f"]
    assert score_interesses(a, b) == pytest.approx(20.0)

def test_score_objetivo_iguais():
    from connect_ai.scoring import score_objetivo
    assert score_objetivo("namoro", "namoro") == pytest.approx(100.0)

def test_score_objetivo_diferentes():
    from connect_ai.scoring import score_objetivo
    assert score_objetivo("namoro", "amizade") == pytest.approx(0.0)

def test_score_idade_iguais():
    from connect_ai.scoring import score_idade
    assert score_idade(27, 27) == pytest.approx(100.0)

def test_score_idade_cinco_anos():
    from connect_ai.scoring import score_idade
    assert score_idade(27, 32) == pytest.approx(90.0)

def test_score_idade_truncado_zero():
    from connect_ai.scoring import score_idade
    # 100 - 50*2 = 0, nao negativo
    assert score_idade(20, 70) == pytest.approx(0.0)

def test_score_idade_quarenta_e_nove_anos():
    from connect_ai.scoring import score_idade
    # 100 - 49*2 = 2
    assert score_idade(20, 69) == pytest.approx(2.0)

def test_score_geografia_mesma_cidade():
    from connect_ai.scoring import score_geografia
    assert score_geografia("Sao Paulo", "Sao Paulo") == pytest.approx(100.0)

def test_score_geografia_cidades_diferentes():
    from connect_ai.scoring import score_geografia
    assert score_geografia("Sao Paulo", "Rio de Janeiro") == pytest.approx(0.0)

def test_calcular_score_retorna_dict_com_chaves():
    from connect_ai.scoring import calcular_score
    resultado = calcular_score(
        distancia_coseno=0.0,
        interesses_a=["musica"],
        interesses_b=["musica"],
        objetivo_a="namoro",
        objetivo_b="namoro",
        idade_a=27,
        idade_b=27,
        cidade_a="Sao Paulo",
        cidade_b="Sao Paulo",
    )
    assert isinstance(resultado, dict)
    for chave in ["score_final", "score_semantico", "score_interesses",
                  "score_objetivo", "score_idade", "score_geografia"]:
        assert chave in resultado, f"Chave '{chave}' ausente no resultado"

def test_calcular_score_composicao_pesos():
    from connect_ai.scoring import calcular_score
    # Caso controlado: semantico=100, interesses=20 (4 em comum), objetivo=100, idade=100, geo=100
    # score_final = 100*0.60 + 20*0.20 + 100*0.10 + 100*0.05 + 100*0.05
    #             = 60 + 4 + 10 + 5 + 5 = 84.0
    resultado = calcular_score(
        distancia_coseno=0.0,
        interesses_a=["a", "b", "c", "d"],
        interesses_b=["a", "b", "c", "d"],
        objetivo_a="namoro",
        objetivo_b="namoro",
        idade_a=27,
        idade_b=27,
        cidade_a="SP",
        cidade_b="SP",
    )
    assert resultado["score_final"] == pytest.approx(84.0)
    assert resultado["score_semantico"] == pytest.approx(100.0)
    assert resultado["score_interesses"] == pytest.approx(20.0)

def test_calcular_score_gate_85_com_cinco_interesses():
    from connect_ai.scoring import calcular_score
    # 5+ interesses em comum: interesses=20, semantico=100, objetivo=100, idade=100, geo=100
    # score_final = 60 + 4 + 10 + 5 + 5 = 84 ... mas distancia 0 da 100 semantico
    # Com 5 interesses: min(5*5,20)=20 -> mesmo resultado 84
    # O gate 85 requer semantico elevado + demais fatores -- testado via integracao em test_consumo.py
    resultado = calcular_score(
        distancia_coseno=0.0,
        interesses_a=["a", "b", "c", "d", "e"],
        interesses_b=["a", "b", "c", "d", "e"],
        objetivo_a="namoro",
        objetivo_b="namoro",
        idade_a=27,
        idade_b=27,
        cidade_a="SP",
        cidade_b="SP",
    )
    # interesses truncado em 20, semantico=100 -> score_final=84
    assert resultado["score_final"] == pytest.approx(84.0)

def test_calcular_score_final_nao_negativo():
    from connect_ai.scoring import calcular_score
    resultado = calcular_score(
        distancia_coseno=2.0,
        interesses_a=[],
        interesses_b=["a"],
        objetivo_a="namoro",
        objetivo_b="amizade",
        idade_a=18,
        idade_b=99,
        cidade_a="A",
        cidade_b="B",
    )
    assert resultado["score_final"] >= 0.0
```

4. Todo texto de docstring, comentarios e mensagens de erro em PT-BR.
5. Arquivo deve ter `import pytest` no topo (nao lazy — pytest e sempre necessario).
  </action>

  <verify>
    <automated>cd "C:/Users/power/OneDrive/Documentos/PROJETOS_/Trabalhos/Faculdade/CP5_PLN" && python -m pytest tests/test_scoring.py --tb=short -q 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - tests/test_scoring.py existe em disco
    - `python -m pytest tests/test_scoring.py` termina com erros (nao com zero falhas)
    - A saida contem "ImportError" ou "ModuleNotFoundError" referenciando "connect_ai.scoring"
    - O arquivo contem exatamente as funcoes: test_score_semantico_distancia_zero, test_score_semantico_distancia_um, test_score_semantico_distancia_dois, test_score_semantico_distancia_meio, test_score_interesses_zero_comum, test_score_interesses_um_comum, test_score_interesses_quatro_comum, test_score_interesses_truncado_em_vinte, test_score_objetivo_iguais, test_score_objetivo_diferentes, test_score_idade_iguais, test_score_idade_cinco_anos, test_score_idade_truncado_zero, test_score_idade_quarenta_e_nove_anos, test_score_geografia_mesma_cidade, test_score_geografia_cidades_diferentes, test_calcular_score_retorna_dict_com_chaves, test_calcular_score_composicao_pesos, test_calcular_score_gate_85_com_cinco_interesses, test_calcular_score_final_nao_negativo
    - Cada funcao de teste contem `from connect_ai.scoring import` DENTRO do corpo da funcao (nao no topo)
    - `grep -c "from connect_ai.scoring import" tests/test_scoring.py` retorna >= 10
    - Nenhum import de connect_ai.scoring no topo do arquivo
  </acceptance_criteria>

  <done>
    tests/test_scoring.py criado com 20 testes RED, todos falhando com ImportError pois
    connect_ai/scoring.py ainda nao existe. Contratos dos 5 fatores e da composicao ponderada
    60/20/10/5/5 estao documentados como testes verificaveis.
  </done>
</task>

<task type="tdd">
  <name>Tarefa 2: Testes RED para pipeline de consumo e TEST-03 (TEST-02, TEST-03, CONS-01, CONS-02, CONS-03)</name>
  <files>tests/test_consumo.py</files>

  <read_first>
    - connect_ai/agentes.py (estrutura de AgentState, pipeline_consumo, agente_casamenteiro)
    - connect_ai/repositorio.py (assinatura de Repositorio, ResultadoBusca, metodos buscar/inserir)
    - connect_ai/ingestao.py (ingerir_lote — sera usado na fixture de integracao)
    - connect_ai/schema.py (Perfil, construir_documento_semantico)
    - tests/test_repositorio.py (padrao de fixture com tmp_path, _perfil helper, DIM=16)
    - tests/test_agentes.py (padrao de imports lazy, fixture state_inicial)
  </read_first>

  <behavior>
    <!-- RED: testes de consumo falham pois buscar_matches nao existe ainda -->

    GRUPO A — Testes unitarios de buscar_matches (CONS-01..03, SCR-04..05):
    - buscar_matches existe e aceita (perfil, colecao)
    - buscar_matches retorna lista de dicts
    - Cada dict contem as chaves: id, score, nome, cidade, idade, score_semantico, score_interesses, score_objetivo, score_idade, score_geografia
    - Todos os matches retornados tem score >= 85.0
    - Maximo de 10 matches retornados
    - Filtro hard de genero: candidatos com genero errado nao aparecem (ex: PERFIL_TESTE busca "masculino", candidatos "feminino" excluidos)
    - Filtro hard de objetivo: candidatos com objetivo diferente nao aparecem

    GRUPO B — Teste de integracao end-to-end (TEST-02 — GATE CRITICO):
    - Inserir seed data completo (ingerir_lote com gerar_pool_perfis())
    - Chamar buscar_matches(PERFIL_TESTE, colecao)
    - Resultado tem exatamente >= 10 matches com score >= 85.0
    - Verificacao: len(matches) >= 10 e todos os scores >= 85.0

    GRUPO C — Teste de repositorio com filtros (TEST-03):
    - buscar com filtro where={"objetivo": "namoro", "genero": "masculino"} retorna apenas matches corretos
    - inserir_lote + buscar com n_resultados=30 retorna ate 30 resultados
    - resetar limpa o banco corretamente (ja coberto em test_repositorio.py mas duplicar para completude de TEST-03)
  </behavior>

  <action>
Criar tests/test_consumo.py com os seguintes requisitos:

1. IMPORTS LAZY dentro de cada funcao para garantir RED individual.
   Excecao: `import pytest` e `from connect_ai.seed_data import PERFIL_TESTE, gerar_pool_perfis`
   podem estar no topo (estes modulos ja existem e nao causam ImportError).

2. Fixture `colecao_temp(tmp_path)` que cria Repositorio isolado (padrao de test_repositorio.py):
```python
@pytest.fixture
def colecao_temp(tmp_path):
    from connect_ai.repositorio import Repositorio
    return Repositorio(
        diretorio=str(tmp_path / "chroma_consumo_test"),
        nome_colecao="test_consumo",
    )
```

3. Fixture `colecao_com_seed(tmp_path)` que insere o lote completo de seed data (para TEST-02):
```python
@pytest.fixture
def colecao_com_seed(tmp_path):
    from connect_ai.repositorio import Repositorio
    from connect_ai.ingestao import ingerir_lote
    repo = Repositorio(
        diretorio=str(tmp_path / "chroma_seed_test"),
        nome_colecao="test_seed_completo",
    )
    perfis = gerar_pool_perfis()
    ingerir_lote(perfis, repo)
    return repo
```

4. Funcoes de teste:

```python
def test_buscar_matches_existe(colecao_temp):
    """buscar_matches deve ser importavel de connect_ai.agentes."""
    from connect_ai.agentes import buscar_matches  # RED: ImportError
    assert callable(buscar_matches)

def test_buscar_matches_retorna_lista(colecao_temp):
    """buscar_matches deve retornar lista."""
    from connect_ai.agentes import buscar_matches
    resultado = buscar_matches(PERFIL_TESTE, colecao_temp)
    assert isinstance(resultado, list)

def test_buscar_matches_estrutura_dict(colecao_com_seed):
    """Cada match deve conter as chaves de exibicao e breakdown."""
    from connect_ai.agentes import buscar_matches
    matches = buscar_matches(PERFIL_TESTE, colecao_com_seed)
    chaves_esperadas = {
        "id", "score", "nome", "cidade", "idade",
        "score_semantico", "score_interesses",
        "score_objetivo", "score_idade", "score_geografia",
    }
    for m in matches:
        for chave in chaves_esperadas:
            assert chave in m, f"Chave '{chave}' ausente no match {m.get('id')}"

def test_buscar_matches_score_minimo_85(colecao_com_seed):
    """Todos os matches retornados devem ter score >= 85.0."""
    from connect_ai.agentes import buscar_matches
    matches = buscar_matches(PERFIL_TESTE, colecao_com_seed)
    for m in matches:
        assert m["score"] >= 85.0, (
            f"Match id={m['id']} tem score={m['score']:.2f} abaixo de 85"
        )

def test_buscar_matches_maximo_dez(colecao_com_seed):
    """buscar_matches deve retornar no maximo 10 matches."""
    from connect_ai.agentes import buscar_matches
    matches = buscar_matches(PERFIL_TESTE, colecao_com_seed)
    assert len(matches) <= 10, f"Retornou {len(matches)} matches, esperado <= 10"

def test_gate_critico_dez_matches_acima_85(colecao_com_seed):
    """GATE CRITICO (TEST-02): pipeline retorna >= 10 matches com score >= 85 para PERFIL_TESTE."""
    from connect_ai.agentes import buscar_matches
    matches = buscar_matches(PERFIL_TESTE, colecao_com_seed)
    acima_85 = [m for m in matches if m["score"] >= 85.0]
    assert len(acima_85) >= 10, (
        f"Gate critico FALHOU: apenas {len(acima_85)} matches com score >= 85 "
        f"(esperado >= 10). Verificar seed data e formula de scoring. "
        f"Scores obtidos: {sorted([m['score'] for m in matches], reverse=True)}"
    )

def test_filtro_hard_genero(colecao_com_seed):
    """Candidatos com genero incompativel com genero_preferido do solicitante nao aparecem."""
    from connect_ai.agentes import buscar_matches
    # PERFIL_TESTE tem genero_preferido="masculino"
    # logo candidatos feminino nao devem aparecer
    matches = buscar_matches(PERFIL_TESTE, colecao_com_seed)
    for m in matches:
        # metadata do candidato deve ter genero="masculino"
        assert m.get("genero", "masculino") == "masculino", (
            f"Match id={m['id']} tem genero errado: {m.get('genero')}"
        )

def test_filtro_hard_objetivo(colecao_com_seed):
    """Candidatos com objetivo diferente do solicitante nao aparecem nos matches."""
    from connect_ai.agentes import buscar_matches
    # PERFIL_TESTE tem objetivo="namoro"
    matches = buscar_matches(PERFIL_TESTE, colecao_com_seed)
    for m in matches:
        assert m.get("objetivo", "namoro") == "namoro", (
            f"Match id={m['id']} tem objetivo errado: {m.get('objetivo')}"
        )

def test_repositorio_busca_filtrada_por_genero(colecao_temp):
    """TEST-03: where com genero filtra corretamente no ChromaDB."""
    from connect_ai.repositorio import Repositorio
    from connect_ai.ingestao import ingerir_perfil
    from connect_ai.schema import Perfil
    # Inserir 1 masculino e 1 feminino
    perfil_m = Perfil(
        id="test-masc-001",
        nome="Carlos Teste",
        idade=28,
        cidade="Sao Paulo",
        genero="masculino",
        genero_preferido="feminino",
        faixa_etaria_pref=(20, 35),
        objetivo="namoro",
        bio="Bio masculino teste consumo.",
        interesses=["musica", "esportes"],
    )
    perfil_f = Perfil(
        id="test-fem-001",
        nome="Carla Teste",
        idade=26,
        cidade="Sao Paulo",
        genero="feminino",
        genero_preferido="masculino",
        faixa_etaria_pref=(20, 35),
        objetivo="namoro",
        bio="Bio feminino teste consumo.",
        interesses=["musica", "viagem"],
    )
    ingerir_perfil(perfil_m, colecao_temp)
    ingerir_perfil(perfil_f, colecao_temp)
    assert colecao_temp.contar() == 2

    # Buscar apenas masculinos
    from connect_ai.repositorio import ResultadoBusca
    resultados = colecao_temp.buscar(
        embedding_query=[0.5] * 768,
        n_resultados=10,
        filtros={"genero": "masculino"},
    )
    ids_encontrados = {r.id for r in resultados}
    assert "test-masc-001" in ids_encontrados
    assert "test-fem-001" not in ids_encontrados

def test_repositorio_busca_top30(colecao_com_seed):
    """TEST-03: buscar com n_resultados=30 retorna ate 30 resultados do seed."""
    resultados = colecao_com_seed.buscar(
        embedding_query=[0.5] * 768,
        n_resultados=30,
    )
    assert len(resultados) <= 30
    assert len(resultados) > 0
```

5. Todo texto de docstrings e comentarios em PT-BR.
6. `import pytest` no topo. `from connect_ai.seed_data import PERFIL_TESTE, gerar_pool_perfis` no topo (ja existem).
  </action>

  <verify>
    <automated>cd "C:/Users/power/OneDrive/Documentos/PROJETOS_/Trabalhos/Faculdade/CP5_PLN" && python -m pytest tests/test_consumo.py --tb=short -q 2>&1 | head -40</automated>
  </verify>

  <acceptance_criteria>
    - tests/test_consumo.py existe em disco
    - `python -m pytest tests/test_consumo.py` termina com erros (nao com zero falhas)
    - A saida contem "ImportError" ou "cannot import name 'buscar_matches'" para os testes de consumo
    - O arquivo contem as funcoes: test_buscar_matches_existe, test_buscar_matches_retorna_lista, test_buscar_matches_estrutura_dict, test_buscar_matches_score_minimo_85, test_buscar_matches_maximo_dez, test_gate_critico_dez_matches_acima_85, test_filtro_hard_genero, test_filtro_hard_objetivo, test_repositorio_busca_filtrada_por_genero, test_repositorio_busca_top30
    - O arquivo contem fixture `colecao_temp` com tmp_path
    - O arquivo contem fixture `colecao_com_seed` com ingerir_lote(gerar_pool_perfis(), repo)
    - Os imports de connect_ai.agentes.buscar_matches estao DENTRO das funcoes (lazy)
    - `python -m pytest tests/test_agentes.py tests/test_repositorio.py -q` continua passando (suite existente nao quebrada)
  </acceptance_criteria>

  <done>
    tests/test_consumo.py criado com 10 testes RED. Os testes de buscar_matches falham com
    ImportError. Os testes de repositorio (TEST-03) podem passar parcialmente (Repositorio ja existe).
    O gate critico test_gate_critico_dez_matches_acima_85 esta documentado com mensagem de erro
    informativa caso falhe na Wave 2.
  </done>
</task>

</tasks>

<verification>
Verificacao da Wave 1 (RED):
1. `python -m pytest tests/test_scoring.py -q` — DEVE FALHAR (ImportError connect_ai.scoring)
2. `python -m pytest tests/test_consumo.py -q` — DEVE FALHAR (ImportError buscar_matches)
3. `python -m pytest tests/test_agentes.py tests/test_repositorio.py tests/test_ingestao.py -q` — DEVE PASSAR (suite existente intacta)
4. `grep -c "from connect_ai.scoring import" tests/test_scoring.py` — retorna >= 10
5. Ambos os arquivos existem em tests/
</verification>

<success_criteria>
- tests/test_scoring.py existe com 20 testes, todos falhando com ImportError
- tests/test_consumo.py existe com 10 testes, testes de buscar_matches falhando com ImportError
- Suite existente (test_agentes, test_repositorio, test_ingestao, test_schema, test_grafo) continua 100% green
- Contratos de calcular_score, score_semantico, score_interesses, score_objetivo, score_idade, score_geografia e buscar_matches estao documentados como testes verificaveis
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/05-pipeline-de-consumo-e-scoring/05-01-SUMMARY.md`
</output>
