---
phase: 05-pipeline-de-consumo-e-scoring
plan: 02
type: execute
wave: 2
depends_on:
  - "05-01"
files_modified:
  - connect_ai/scoring.py
  - connect_ai/agentes.py
  - connect_ai/repositorio.py
autonomous: true
requirements:
  - SCR-01
  - SCR-02
  - SCR-03
  - SCR-04
  - SCR-05
  - CONS-01
  - CONS-02
  - CONS-03
  - TEST-01
  - TEST-02
  - TEST-03

must_haves:
  truths:
    - "connect_ai/scoring.py existe com as 6 funcoes publicas (5 fatores + calcular_score)"
    - "connect_ai/agentes.py exporta buscar_matches que orquestra o pipeline end-to-end"
    - "_calcular_score_stub em agentes.py e substituido por chamada a calcular_score de scoring.py"
    - "connect_ai/repositorio.py _metadata_de_perfil grava campo interesses_csv como string CSV dos interesses do perfil"
    - "python -m pytest tests/test_scoring.py retorna 20 passed"
    - "python -m pytest tests/test_consumo.py retorna 10 passed incluindo test_gate_critico_dez_matches_acima_85"
    - "Suite completa (72+ testes) continua 100% green"
  artifacts:
    - path: "connect_ai/scoring.py"
      provides: "Modulo de scoring ponderado 60/20/10/5/5 com os 5 fatores"
      exports:
        ["score_semantico", "score_interesses", "score_objetivo", "score_idade", "score_geografia", "calcular_score"]
    - path: "connect_ai/agentes.py"
      provides: "buscar_matches orquestrando pipeline completo de consumo"
      contains: "def buscar_matches"
    - path: "connect_ai/repositorio.py"
      provides: "Campo interesses_csv no metadata de cada perfil para score_interesses"
      contains: "interesses_csv"
  key_links:
    - from: "connect_ai/agentes.py"
      to: "connect_ai/scoring.py"
      via: "from connect_ai.scoring import calcular_score"
      pattern: "from connect_ai\\.scoring import calcular_score"
    - from: "connect_ai/agentes.py"
      to: "connect_ai/repositorio.py"
      via: "colecao.buscar(embedding_query, n_resultados=30, filtros=where)"
      pattern: "colecao\\.buscar"
    - from: "connect_ai/agentes.py"
      to: "connect_ai/ingestao.py"
      via: "_gerar_embedding(construir_documento_semantico(perfil))"
      pattern: "_gerar_embedding"
---

<objective>
Implementar connect_ai/scoring.py e atualizar connect_ai/agentes.py com buscar_matches,
tornando todos os testes RED da Wave 1 GREEN.

Proposito: Entregar o gate critico da Fase 5 — pipeline end-to-end que retorna >= 10 matches
com score >= 85 para PERFIL_TESTE com breakdown explicito dos 5 fatores.

Output: scoring.py (novo modulo) + agentes.py (atualizado com buscar_matches e _calcular_score
substituido) + repositorio.py (atualizado com interesses_csv no metadata), com 30 testes
passando no total (20 scoring + 10 consumo).
</objective>

<execution_context>
@C:/Users/power/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/power/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@connect_ai/schema.py
@connect_ai/agentes.py
@connect_ai/repositorio.py
@connect_ai/ingestao.py
@tests/test_scoring.py
@tests/test_consumo.py

<interfaces>
<!-- Interfaces das quais scoring.py e agentes.py dependem -->

<!-- De connect_ai/schema.py (JA EXISTE): -->
```python
class Perfil(BaseModel):
    id: str
    nome: str
    idade: int
    cidade: str
    genero: Genero          # "feminino" | "masculino" | "nao_binario" | "outro"
    genero_preferido: GeneroPreferido  # inclui "todos"
    faixa_etaria_pref: Tuple[int, int]
    objetivo: Objetivo      # "namoro" | "casual" | "amizade"
    bio: str
    interesses: List[str]
    personalidade_ia: Optional[str] = None

def construir_documento_semantico(perfil: Perfil) -> str: ...
```

<!-- De connect_ai/repositorio.py (JA EXISTE): -->
```python
class ResultadoBusca:
    id: str
    documento: str
    metadata: Dict[str, Any]  # chaves: idade, cidade, genero, genero_preferido, faixa_etaria_min, faixa_etaria_max, objetivo, nome
    distancia: float          # cosine distance [0, 2]

class Repositorio:
    def buscar(self, embedding_query, n_resultados=30, filtros=None) -> List[ResultadoBusca]: ...
    def inserir(self, perfil, embedding) -> None: ...
    def contar(self) -> int: ...
    def resetar(self) -> None: ...
```

<!-- De connect_ai/ingestao.py (JA EXISTE): -->
```python
def _gerar_embedding(texto: str) -> list:
    """Retorna lista de 768 floats. Sem API_KEY: mock md5 deterministico."""
```

<!-- De connect_ai/agentes.py (JA EXISTE — NAO REESCREVER, APENAS ADICIONAR/SUBSTITUIR): -->
```python
class AgentState(TypedDict):
    perfil: Perfil
    candidatos: List[Dict[str, Any]]
    matches: List[Dict[str, Any]]
    justificativas: Dict[str, str]
    erro: Optional[str]

def _calcular_score_stub(candidato, perfil_ref) -> float:
    """SUBSTITUIR na Fase 5 — atualmente retorna 90.0 hardcoded."""

def agente_casamenteiro(state: AgentState) -> AgentState: ...
def agente_perfilador(state: AgentState) -> AgentState: ...
def agente_rag_justificador(state: AgentState) -> AgentState: ...
# pipeline_consumo: grafo LangGraph compilado (NAO TOCAR)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Criar connect_ai/scoring.py com os 5 fatores e composicao ponderada (SCR-01..SCR-05, TEST-01)</name>
  <files>connect_ai/scoring.py</files>

  <read_first>
    - tests/test_scoring.py (contratos exatos que esta implementacao deve satisfazer — 20 testes)
    - connect_ai/schema.py (tipos Perfil, campos interesses/objetivo/idade/cidade)
    - connect_ai/repositorio.py (ResultadoBusca.distancia — escala [0,2])
  </read_first>

  <behavior>
    <!-- Contratos derivados dos testes RED em tests/test_scoring.py -->
    - score_semantico(0.0) == 100.0
    - score_semantico(1.0) == 50.0
    - score_semantico(2.0) == 0.0
    - score_interesses(["a"],["b"]) == 0.0
    - score_interesses(["a","b"],["a","c"]) == 5.0
    - score_interesses(4 iguais) == 20.0
    - score_interesses(5+ iguais) == 20.0 (truncado)
    - score_objetivo(iguais) == 100.0, score_objetivo(diferentes) == 0.0
    - score_idade(27,27) == 100.0, score_idade(27,32) == 90.0, score_idade(20,70) == 0.0
    - score_geografia(iguais) == 100.0, score_geografia(diferentes) == 0.0
    - calcular_score retorna dict com 6 chaves
    - score_final == semantico*0.60 + interesses*0.20 + objetivo*0.10 + idade*0.05 + geo*0.05
  </behavior>

  <action>
Criar connect_ai/scoring.py NOVO modulo com o seguinte conteudo exato:

```python
"""Modulo de scoring de compatibilidade do CONNECT.AI.

Implementa o calculo ponderado 60/20/10/5/5 (SCR-01 a SCR-05):
  - score_semantico  (60%): similaridade coseno via distancia ChromaDB
  - score_interesses (20%): interesses em comum, ate 4 valem 20pts
  - score_objetivo   (10%): objetivo identico = 100pts, diferente = 0
  - score_idade      ( 5%): penalidade de 2pts por ano de diferenca
  - score_geografia  ( 5%): mesma cidade = 100pts, diferente = 0

A funcao `calcular_score` compoe os 5 fatores e retorna o breakdown
completo, necessario para exibicao no front (SCR-05) e para o gate
critico de 10 matches >= 85 (TEST-02, ACC-02).

PITFALL CRITICO: ChromaDB retorna DISTANCIA coseno em [0,2], nao similaridade.
A conversao correta e: similaridade = (1 - distancia/2) * 100
"""
from __future__ import annotations


def score_semantico(distancia_coseno: float) -> float:
    """Converte distancia coseno [0,2] em score de similaridade [0,100].

    Formula: (1 - distancia / 2) * 100

    ChromaDB retorna DISTANCIA (0 = identico, 2 = oposto). Esta funcao
    converte para score onde 100 = identico e 0 = oposto.

    Args:
        distancia_coseno: Distancia coseno em [0.0, 2.0] retornada pelo ChromaDB.

    Returns:
        Score de similaridade em [0.0, 100.0].
    """
    return (1.0 - distancia_coseno / 2.0) * 100.0


def score_interesses(interesses_a: list, interesses_b: list) -> float:
    """Calcula score baseado em interesses em comum (max 20 pontos).

    Cada interesse em comum vale 5 pontos, truncado em 20.
    Formula: min(len(set(a) & set(b)) * 5, 20)

    Quatro ou mais interesses em comum atingem o maximo de 20 pontos.
    Comparacao case-sensitive (como gravado no ChromaDB).

    Args:
        interesses_a: Lista de interesses do perfil solicitante.
        interesses_b: Lista de interesses do candidato.

    Returns:
        Score de interesses em [0.0, 20.0].
    """
    em_comum = len(set(interesses_a) & set(interesses_b))
    return float(min(em_comum * 5, 20))


def score_objetivo(objetivo_a: str, objetivo_b: str) -> float:
    """Retorna 100.0 se os objetivos forem identicos, 0.0 caso contrario.

    Objetivos validos: "namoro", "casual", "amizade" (Literal do schema).
    Comparacao case-sensitive.

    Args:
        objetivo_a: Objetivo do perfil solicitante.
        objetivo_b: Objetivo do candidato.

    Returns:
        100.0 se objetivo_a == objetivo_b, 0.0 caso contrario.
    """
    return 100.0 if objetivo_a == objetivo_b else 0.0


def score_idade(idade_a: int, idade_b: int) -> float:
    """Calcula score de compatibilidade de idade (penalidade por diferenca).

    Formula: max(0.0, min(100.0, 100 - abs(idade_a - idade_b) * 2))

    Cada ano de diferenca desconta 2 pontos. Truncado em [0, 100]:
    diferenca de 50 anos ou mais resulta em 0 pontos.

    Args:
        idade_a: Idade do perfil solicitante.
        idade_b: Idade do candidato.

    Returns:
        Score de idade em [0.0, 100.0].
    """
    diferenca = abs(idade_a - idade_b)
    return float(max(0.0, min(100.0, 100.0 - diferenca * 2.0)))


def score_geografia(cidade_a: str, cidade_b: str) -> float:
    """Retorna 100.0 se as cidades forem identicas, 0.0 caso contrario.

    Comparacao case-sensitive (como gravado no ChromaDB).
    Versoes futuras podem usar distancia geografica real (V2).

    Args:
        cidade_a: Cidade do perfil solicitante.
        cidade_b: Cidade do candidato.

    Returns:
        100.0 se cidade_a == cidade_b, 0.0 caso contrario.
    """
    return 100.0 if cidade_a == cidade_b else 0.0


def calcular_score(
    distancia_coseno: float,
    interesses_a: list,
    interesses_b: list,
    objetivo_a: str,
    objetivo_b: str,
    idade_a: int,
    idade_b: int,
    cidade_a: str,
    cidade_b: str,
) -> dict:
    """Calcula score de compatibilidade ponderado 60/20/10/5/5.

    Composicao:
      score_final = semantico * 0.60
                  + interesses * 0.20
                  + objetivo * 0.10
                  + idade * 0.05
                  + geografia * 0.05

    Retorna o breakdown completo dos 5 fatores para exibicao no front
    (SCR-05) e para rastreabilidade do gate critico (TEST-02).

    Nota sobre o threshold >= 85: O score maxximo com score_interesses
    truncado em 20 (nao 100) e pesos corretos e:
      60 + 4 + 10 + 5 + 5 = 84 (4 interesses em comum, perfil identico).
    O gate >= 85 so e atingivel quando a similaridade semantica e
    suficientemente alta E ha sobreposicao de interesses -- confirmando
    que o seed data calibrado na Fase 2 e essencial para o gate.

    Args:
        distancia_coseno: Distancia coseno [0.0, 2.0] do ChromaDB.
        interesses_a: Lista de interesses do solicitante.
        interesses_b: Lista de interesses do candidato.
        objetivo_a: Objetivo do solicitante ("namoro"/"casual"/"amizade").
        objetivo_b: Objetivo do candidato.
        idade_a: Idade do solicitante.
        idade_b: Idade do candidato.
        cidade_a: Cidade do solicitante.
        cidade_b: Cidade do candidato.

    Returns:
        Dict com chaves:
          score_final (float): Score composto em [0.0, 100.0].
          score_semantico (float): Componente semantico [0.0, 100.0].
          score_interesses (float): Componente de interesses [0.0, 20.0].
          score_objetivo (float): Componente de objetivo (0 ou 100).
          score_idade (float): Componente de idade [0.0, 100.0].
          score_geografia (float): Componente de geografia (0 ou 100).
    """
    s_semantico = score_semantico(distancia_coseno)
    s_interesses = score_interesses(interesses_a, interesses_b)
    s_objetivo = score_objetivo(objetivo_a, objetivo_b)
    s_idade = score_idade(idade_a, idade_b)
    s_geografia = score_geografia(cidade_a, cidade_b)

    score_final = (
        s_semantico * 0.60
        + s_interesses * 0.20
        + s_objetivo * 0.10
        + s_idade * 0.05
        + s_geografia * 0.05
    )

    return {
        "score_final": float(score_final),
        "score_semantico": float(s_semantico),
        "score_interesses": float(s_interesses),
        "score_objetivo": float(s_objetivo),
        "score_idade": float(s_idade),
        "score_geografia": float(s_geografia),
    }
```

VERIFICAR APOS CRIACAO:
- `python -m pytest tests/test_scoring.py -v` deve retornar 20 passed, 0 failed
- Se algum teste falhar, revisar a formula do fator correspondente
  </action>

  <verify>
    <automated>cd "C:/Users/power/OneDrive/Documentos/PROJETOS_/Trabalhos/Faculdade/CP5_PLN" && python -m pytest tests/test_scoring.py -v 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - connect_ai/scoring.py existe em disco
    - `python -m pytest tests/test_scoring.py -v` retorna "20 passed" na saida
    - `grep "def score_semantico" connect_ai/scoring.py` retorna resultado
    - `grep "def score_interesses" connect_ai/scoring.py` retorna resultado
    - `grep "def score_objetivo" connect_ai/scoring.py` retorna resultado
    - `grep "def score_idade" connect_ai/scoring.py` retorna resultado
    - `grep "def score_geografia" connect_ai/scoring.py` retorna resultado
    - `grep "def calcular_score" connect_ai/scoring.py` retorna resultado
    - `python -c "from connect_ai.scoring import calcular_score; print(calcular_score(0.0,['a'],['a'],'namoro','namoro',27,27,'SP','SP'))"` imprime dict com score_final
    - Nenhum teste previamente passando e quebrado
  </acceptance_criteria>

  <done>
    connect_ai/scoring.py criado com 6 funcoes publicas. 20 testes em tests/test_scoring.py passam.
    Formulas: score_semantico=(1-d/2)*100, score_interesses=min(em_comum*5,20),
    score_objetivo=100 se igual, score_idade=max(0,100-diff*2), score_geografia=100 se igual.
    Composicao: 60/20/10/5/5.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Tarefa 2: Implementar buscar_matches em agentes.py, adicionar interesses_csv em repositorio.py e substituir _calcular_score_stub (CONS-01..03, SCR-04..05, TEST-02)</name>
  <files>connect_ai/agentes.py, connect_ai/repositorio.py</files>

  <read_first>
    - connect_ai/agentes.py (estado atual — AgentState, agente_casamenteiro, _calcular_score_stub, pipeline_consumo)
    - connect_ai/repositorio.py (estado atual — metodo _metadata_de_perfil e todas as suas chaves existentes, para adicionar interesses_csv sem quebrar testes existentes)
    - connect_ai/scoring.py (recem criado — assinatura de calcular_score)
    - connect_ai/ingestao.py (_gerar_embedding — importar lazily dentro de buscar_matches)
    - connect_ai/schema.py (Perfil — campos genero_preferido, objetivo, idade, cidade, interesses)
    - tests/test_consumo.py (contratos de buscar_matches — chaves esperadas, filtros, gate 85)
  </read_first>

  <behavior>
    <!-- Contratos derivados dos testes RED em tests/test_consumo.py -->
    - buscar_matches(PERFIL_TESTE, colecao_com_seed) retorna lista
    - Cada elemento tem chaves: id, score, nome, cidade, idade, score_semantico, score_interesses, score_objetivo, score_idade, score_geografia
    - Todos os scores >= 85.0
    - Maximo 10 matches
    - GATE: len >= 10 matches com score >= 85 para PERFIL_TESTE no seed completo
    - Filtro hard genero: so candidatos com genero == perfil.genero_preferido (exceto "todos")
    - Filtro hard objetivo: so candidatos com objetivo == perfil.objetivo
    - repositorio.py grava interesses_csv no metadata de cada perfil
  </behavior>

  <action>
ATENCAO: Ler connect_ai/agentes.py E connect_ai/repositorio.py completos antes de editar.
NAO reescrever nenhum dos arquivos inteiros.
Fazer apenas as seguintes alteracoes cirurgicas:

### ARQUIVO 1: connect_ai/repositorio.py

Localizar o metodo `_metadata_de_perfil` (ou equivalente que monta o dict de metadata antes
de inserir no ChromaDB). Verificar quais chaves ja existem (ex: idade, cidade, genero,
genero_preferido, faixa_etaria_min, faixa_etaria_max, objetivo, nome).

Adicionar o campo `interesses_csv` ao dict retornado:
```python
"interesses_csv": ",".join(perfil.interesses),  # ex: "musica,viagem,leitura"
```

ChromaDB nao aceita listas como valores de metadata — a string CSV e a representacao correta.
Verificar que nenhum teste existente de repositorio quebra apos esta adicao executando:
`python -m pytest tests/test_repositorio.py -q`

### ARQUIVO 2: connect_ai/agentes.py

#### ALTERACAO 2a: Adicionar import de Repositorio no topo (se nao existir)
Adicionar apos `from connect_ai.schema import Perfil` (se Repositorio ainda nao importado):
```python
from connect_ai.repositorio import Repositorio
```
(scoring sera importado lazily dentro de buscar_matches para evitar circular imports)

#### ALTERACAO 2b: Substituir _calcular_score_stub
Substituir a funcao `_calcular_score_stub` existente por:
```python
def _calcular_score_stub(candidato: Dict[str, Any], perfil_ref: Perfil) -> float:
    """DEPRECATED: use calcular_score de connect_ai.scoring diretamente.

    Mantido apenas para compatibilidade com agente_casamenteiro (stub da Fase 3).
    A funcao buscar_matches (Fase 5) usa calcular_score diretamente.
    """
    from connect_ai.scoring import calcular_score
    interesses_b = candidato.get("interesses", [])
    resultado = calcular_score(
        distancia_coseno=candidato.get("distancia_coseno", 1.0),
        interesses_a=list(perfil_ref.interesses),
        interesses_b=list(interesses_b) if isinstance(interesses_b, (list, tuple)) else [],
        objetivo_a=str(perfil_ref.objetivo),
        objetivo_b=str(candidato.get("objetivo", "")),
        idade_a=int(perfil_ref.idade),
        idade_b=int(candidato.get("idade", perfil_ref.idade)),
        cidade_a=str(perfil_ref.cidade),
        cidade_b=str(candidato.get("cidade", "")),
    )
    return resultado["score_final"]
```

#### ALTERACAO 2c: Adicionar funcao buscar_matches ANTES de agente_casamenteiro
Inserir a seguinte funcao nova apos o bloco do stub:

```python
def buscar_matches(perfil_solicitante: Perfil, colecao: "Repositorio") -> List[Dict[str, Any]]:
    """Orquestra o pipeline de consumo end-to-end (CONS-01..03, SCR-04..05).

    Fluxo:
      1. Enriquece o perfil com personalidade_ia via agente_perfilador (determinismo AGT-07).
      2. Gera embedding do documento semantico via _gerar_embedding.
      3. Aplica filtros hard via metadata do ChromaDB (CONS-02):
         - objetivo: so candidatos com mesmo objetivo
         - genero: so candidatos com genero == perfil.genero_preferido (se != "todos")
      4. Busca vetorial Top-30 no ChromaDB (CONS-03, K=30).
      5. Para cada resultado, parseia interesses_csv do metadata e calcula score
         ponderado 60/20/10/5/5 via calcular_score (SCR-01..03).
      6. Filtra score >= 85 e retorna Top-10 ordenado por score desc (SCR-04).
      7. Cada dict de retorno inclui breakdown dos 5 fatores (SCR-05).

    PITFALL: ChromaDB retorna distancia [0,2], nao similaridade. A conversao
    e feita dentro de score_semantico em connect_ai.scoring.

    INTERESSES: Gravados como string CSV ("interesses_csv") no metadata do ChromaDB
    pelo metodo _metadata_de_perfil de repositorio.py. Parseados aqui para lista antes
    de chamar score_interesses.

    Args:
        perfil_solicitante: Perfil de quem busca matches.
        colecao: Instancia de Repositorio com perfis ja ingeridos.

    Returns:
        Lista de ate 10 dicts, cada um contendo:
          id, score, nome, cidade, idade, genero,
          score_semantico, score_interesses, score_objetivo,
          score_idade, score_geografia.
        Ordenada por score decrescente. Vazia se nenhum candidato >= 85.
    """
    from connect_ai.scoring import calcular_score
    from connect_ai.ingestao import _gerar_embedding

    # Passo 1: enriquecer personalidade_ia (determinismo via cache)
    state_enriquecido = agente_perfilador({
        "perfil": perfil_solicitante,
        "candidatos": [],
        "matches": [],
        "justificativas": {},
        "erro": None,
    })
    perfil = state_enriquecido["perfil"]

    # Passo 2: gerar embedding do documento semantico
    from connect_ai.schema import construir_documento_semantico
    texto = construir_documento_semantico(perfil)
    embedding_query = _gerar_embedding(texto)

    # Passo 3: montar filtros hard para ChromaDB (CONS-02)
    filtros: Dict[str, Any] = {"objetivo": str(perfil.objetivo)}
    if perfil.genero_preferido != "todos":
        filtros = {
            "$and": [
                {"objetivo": {"$eq": str(perfil.objetivo)}},
                {"genero": {"$eq": str(perfil.genero_preferido)}},
            ]
        }

    # Passo 4: busca vetorial Top-30 (CONS-03)
    candidatos = colecao.buscar(
        embedding_query=embedding_query,
        n_resultados=30,
        filtros=filtros,
    )

    # Passo 5 e 6: calcular score e filtrar >= 85
    matches = []
    for resultado in candidatos:
        meta = resultado.metadata

        # Parsear interesses_csv gravado por repositorio._metadata_de_perfil
        # Formato: "musica,viagem,leitura" -> ["musica", "viagem", "leitura"]
        interesses_csv = str(meta.get("interesses_csv", ""))
        interesses_b: List[str] = [i.strip() for i in interesses_csv.split(",") if i.strip()]

        breakdown = calcular_score(
            distancia_coseno=float(resultado.distancia),
            interesses_a=list(perfil.interesses),
            interesses_b=interesses_b,
            objetivo_a=str(perfil.objetivo),
            objetivo_b=str(meta.get("objetivo", "")),
            idade_a=int(perfil.idade),
            idade_b=int(meta.get("idade", perfil.idade)),
            cidade_a=str(perfil.cidade),
            cidade_b=str(meta.get("cidade", "")),
        )

        if breakdown["score_final"] >= 85.0:
            matches.append({
                "id": resultado.id,
                "score": breakdown["score_final"],
                "nome": str(meta.get("nome", "")),
                "cidade": str(meta.get("cidade", "")),
                "idade": int(meta.get("idade", 0)),
                "genero": str(meta.get("genero", "")),
                "objetivo": str(meta.get("objetivo", "")),
                "score_semantico": breakdown["score_semantico"],
                "score_interesses": breakdown["score_interesses"],
                "score_objetivo": breakdown["score_objetivo"],
                "score_idade": breakdown["score_idade"],
                "score_geografia": breakdown["score_geografia"],
            })

    # Passo 7: ordenar por score desc e limitar a 10 (SCR-04)
    matches.sort(key=lambda m: m["score"], reverse=True)
    return matches[:10]
```

VERIFICAR APOS EDICOES:
- `python -m pytest tests/test_scoring.py tests/test_consumo.py tests/test_agentes.py tests/test_repositorio.py -v` — todos devem passar
- `grep "interesses_csv" connect_ai/repositorio.py` — deve encontrar o campo adicionado
- `grep "def buscar_matches" connect_ai/agentes.py` — deve encontrar a funcao
  </action>

  <verify>
    <automated>cd "C:/Users/power/OneDrive/Documentos/PROJETOS_/Trabalhos/Faculdade/CP5_PLN" && python -m pytest tests/test_consumo.py tests/test_scoring.py tests/test_agentes.py tests/test_repositorio.py -v 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - `grep "def buscar_matches" connect_ai/agentes.py` retorna resultado
    - `grep "from connect_ai.scoring import calcular_score" connect_ai/agentes.py` retorna resultado (dentro de buscar_matches)
    - `grep "interesses_csv" connect_ai/repositorio.py` retorna resultado (campo adicionado ao _metadata_de_perfil)
    - `grep "interesses_csv" connect_ai/agentes.py` retorna resultado (parseado em buscar_matches)
    - `python -m pytest tests/test_scoring.py -q` retorna "20 passed"
    - `python -m pytest tests/test_consumo.py -q` retorna "10 passed" incluindo "test_gate_critico_dez_matches_acima_85 PASSED"
    - `python -m pytest tests/test_agentes.py -q` continua passando (nenhum teste existente quebrado)
    - `python -m pytest tests/test_repositorio.py -q` continua passando
    - Suite completa: `python -m pytest -q` retorna todos os testes passando (nenhuma regressao)
    - `python -c "from connect_ai.agentes import buscar_matches; print('OK')"` imprime OK
    - Se test_gate_critico_dez_matches_acima_85 FALHAR: PARAR execucao, NAO avançar para Fase 6,
      retornar a Fase 2 para recalibrar seed data conforme procedimento de fallback do ROADMAP.
  </acceptance_criteria>

  <done>
    connect_ai/agentes.py atualizado com buscar_matches implementado. _calcular_score_stub
    substituido por chamada real a calcular_score. connect_ai/repositorio.py atualizado com
    interesses_csv no metadata para suporte ao score_interesses. Gate critico verificado:
    test_gate_critico_dez_matches_acima_85 passa com >= 10 matches com score >= 85 para PERFIL_TESTE.
  </done>
</task>

</tasks>

<verification>
Verificacao final da Wave 2 (GREEN):
1. `python -m pytest tests/test_scoring.py -v` — DEVE RETORNAR 20 passed
2. `python -m pytest tests/test_consumo.py -v` — DEVE RETORNAR 10 passed incluindo test_gate_critico_dez_matches_acima_85
3. `python -m pytest -q` — DEVE RETORNAR todos os testes passando (suite completa >= 82 testes)
4. `grep "def buscar_matches" connect_ai/agentes.py` — deve encontrar a funcao
5. `grep "def calcular_score" connect_ai/scoring.py` — deve encontrar a funcao
6. `grep "interesses_csv" connect_ai/repositorio.py` — deve encontrar o campo no metadata
7. `python -c "from connect_ai.scoring import calcular_score; r=calcular_score(0.5,[],[],  'namoro','namoro',27,27,'SP','SP'); print(f'score={r[\"score_final\"]:.1f}')"` imprime score=75.0
</verification>

<success_criteria>
GATE CRITICO ATINGIDO:
- connect_ai/scoring.py existe com 6 funcoes (score_semantico, score_interesses, score_objetivo, score_idade, score_geografia, calcular_score)
- connect_ai/agentes.py exporta buscar_matches com pipeline completo (filtros hard + busca Top-30 + scoring 60/20/10/5/5 + corte >= 85 + Top-10)
- connect_ai/repositorio.py grava interesses_csv no metadata (viabiliza score_interesses != 0)
- 20 testes de scoring passam (TEST-01, SCR-01..SCR-05)
- 10 testes de consumo passam incluindo o gate critico (TEST-02)
- Testes de repositorio passam com metadata atualizado (TEST-03)
- Suite completa continua 100% green (zero regressoes)
- Fase 5 concluida: avanco para Fase 6 autorizado SOMENTE se gate critico passou
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/05-pipeline-de-consumo-e-scoring/05-02-SUMMARY.md`
</output>
