---
phase: 01-fundacao
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - connect_ai/__init__.py
  - connect_ai/config.py
  - app/__init__.py
  - notebook/.gitkeep
  - relatorio/.gitkeep
  - tests/__init__.py
  - tests/conftest.py
  - tests/test_config.py
  - requirements.txt
  - pyproject.toml
  - .env.example
  - .gitignore
autonomous: true
requirements:
  - ENV-01
  - ENV-02
  - ENV-03
  - ENV-04
  - ENV-05
  - ENV-06

must_haves:
  truths:
    - "O pacote `connect_ai` pode ser instalado em modo editável com `pip install -e .` sem erros"
    - "Importar `from connect_ai.config import obter_chave_api` funciona após a instalação"
    - "Quando a variável `GOOGLE_API_KEY` está ausente, `obter_chave_api()` levanta erro com mensagem clara em PT-BR (não `KeyError` cru)"
    - "`.env` é ignorado pelo git e `.env.example` documenta todas as variáveis necessárias"
  artifacts:
    - path: "connect_ai/__init__.py"
      provides: "Marcador do pacote Python"
      min_lines: 1
    - path: "connect_ai/config.py"
      provides: "Função `obter_chave_api(nome: str) -> str` com erro PT-BR"
      contains: "def obter_chave_api"
    - path: "pyproject.toml"
      provides: "Configuração de instalação editável do pacote `connect_ai`"
      contains: "name = \"connect_ai\""
    - path: "requirements.txt"
      provides: "Dependências fixadas (langgraph, chromadb, google-generativeai, streamlit, pandas, python-dotenv, pytest)"
      contains: "langgraph"
    - path: ".env.example"
      provides: "Documentação das variáveis de ambiente (GOOGLE_API_KEY)"
      contains: "GOOGLE_API_KEY="
    - path: ".gitignore"
      provides: "Ignora .env, __pycache__, chroma_db/, .venv/, etc."
      contains: ".env"
  key_links:
    - from: "connect_ai/config.py"
      to: "python-dotenv (load_dotenv)"
      via: "carregamento do `.env` na inicialização do módulo"
      pattern: "load_dotenv"
    - from: "pyproject.toml"
      to: "connect_ai/ (pacote)"
      via: "declaração de pacote em `[tool.setuptools.packages.find]` ou similar"
      pattern: "connect_ai"
---

<objective>
Estabelecer a fundação física do projeto: criar a estrutura de diretórios do pacote modular `connect_ai/`, declarar todas as dependências obrigatórias, configurar instalação editável via `pyproject.toml`, e implementar o módulo de configuração que carrega chaves de API via `.env` com mensagens de erro em PT-BR.

Propósito: Sem este plano, nenhuma fase posterior consegue importar `from connect_ai...`, executar testes, ou rodar `pip install -e .`. Este é o pré-requisito de absolutamente tudo.

Saída: Pacote Python instalável e importável, configuração segura de chaves de API, repositório com `.gitignore` correto e `.env.example` documentado.
</objective>

<execution_context>
@C:/Users/power/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/power/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@BRIEFING.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Criar estrutura de diretórios e arquivos sentinela</name>
  <files>connect_ai/__init__.py, app/__init__.py, tests/__init__.py, tests/conftest.py, notebook/.gitkeep, relatorio/.gitkeep, .gitignore</files>
  <read_first>
    - .planning/PROJECT.md (decisão "Aplicação Python modular (pacote `connect_ai/`)")
    - .planning/REQUIREMENTS.md (ENV-01)
    - BRIEFING.md (seções 5 e 12 — stack e ambiente)
  </read_first>
  <action>
    Criar a árvore de diretórios completa do projeto:

    1. `connect_ai/__init__.py` — conteúdo:
       ```python
       """Pacote CONNECT.AI — motor de matchmaking semântico (CP5 FIAP)."""

       __version__ = "0.1.0"
       ```

    2. `app/__init__.py` — arquivo vazio (apenas marcador para o front Streamlit).

    3. `tests/__init__.py` — arquivo vazio.

    4. `tests/conftest.py` — conteúdo:
       ```python
       """Configuração compartilhada do pytest para o projeto CONNECT.AI."""

       import sys
       from pathlib import Path

       # Garante que o pacote `connect_ai` é importável durante os testes,
       # mesmo antes do `pip install -e .`.
       RAIZ = Path(__file__).resolve().parent.parent
       if str(RAIZ) not in sys.path:
           sys.path.insert(0, str(RAIZ))
       ```

    5. `notebook/.gitkeep` — arquivo vazio (preserva o diretório no git).

    6. `relatorio/.gitkeep` — arquivo vazio.

    7. `.gitignore` — conteúdo:
       ```
       # Ambientes virtuais
       .venv/
       venv/
       env/

       # Bytecode Python
       __pycache__/
       *.py[cod]
       *$py.class
       *.egg-info/
       build/
       dist/

       # Configuração local com segredos
       .env
       .env.local

       # Persistência local do ChromaDB (decisão de PROJECT.md)
       chroma_db/
       .chroma/

       # Cache de testes e coverage
       .pytest_cache/
       .coverage
       htmlcov/

       # IDE / Editor
       .vscode/
       .idea/
       *.swp

       # SO
       .DS_Store
       Thumbs.db

       # Notebooks (checkpoints, outputs sensíveis)
       .ipynb_checkpoints/
       ```

    NÃO crie subdiretórios ainda dentro de `connect_ai/` (schema.py, repositorio.py, etc.) — esses são responsabilidade dos planos 02 e 03.
  </action>
  <verify>
    <automated>test -f connect_ai/__init__.py && test -f app/__init__.py && test -f tests/__init__.py && test -f tests/conftest.py && test -f notebook/.gitkeep && test -f relatorio/.gitkeep && test -f .gitignore && grep -q "chroma_db/" .gitignore && grep -q "\.env" .gitignore</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `connect_ai/__init__.py` existe e contém `__version__ = "0.1.0"`
    - Arquivo `app/__init__.py` existe (pode estar vazio)
    - Arquivo `tests/__init__.py` existe (pode estar vazio)
    - Arquivo `tests/conftest.py` existe e adiciona a raiz do projeto ao `sys.path`
    - Arquivos `notebook/.gitkeep` e `relatorio/.gitkeep` existem
    - `.gitignore` contém as entradas: `.env`, `chroma_db/`, `__pycache__/`, `.venv/`, `*.egg-info/`, `.pytest_cache/`, `.ipynb_checkpoints/`
    - Comando `python -c "import sys; sys.path.insert(0, '.'); import connect_ai; print(connect_ai.__version__)"` imprime `0.1.0`
  </acceptance_criteria>
  <done>Estrutura mínima do projeto está no disco e o pacote `connect_ai` é importável via `sys.path`.</done>
</task>

<task type="auto">
  <name>Task 2: Declarar dependências e configurar instalação editável</name>
  <files>requirements.txt, pyproject.toml, .env.example</files>
  <read_first>
    - .planning/REQUIREMENTS.md (ENV-02, ENV-03, ENV-05)
    - .planning/PROJECT.md (Stack obrigatória — seção Constraints)
    - BRIEFING.md (seção 5 — Stack tecnológica)
    - connect_ai/__init__.py (criado na Task 1, define `__version__`)
  </read_first>
  <action>
    Criar três arquivos com versões fixadas para reprodutibilidade.

    1. `requirements.txt` — uma dependência por linha, com lower bound (sem upper):
       ```
       # Orquestração de agentes
       langgraph>=0.2.0

       # Banco vetorial
       chromadb>=0.5.0

       # LLM Google (Gemini Pro/Vision + text-embedding-004)
       google-generativeai>=0.8.0

       # Front
       streamlit>=1.36.0

       # Manipulação de dados e tabelas para o relatório
       pandas>=2.2.0

       # Configuração via .env
       python-dotenv>=1.0.0

       # Validação de schema (DATA-01 usa Pydantic)
       pydantic>=2.6.0

       # Testes
       pytest>=8.0.0
       ```

    2. `pyproject.toml` — usar `setuptools` como build-backend (mais simples e estável que `hatch` para este escopo):
       ```toml
       [build-system]
       requires = ["setuptools>=68", "wheel"]
       build-backend = "setuptools.build_meta"

       [project]
       name = "connect_ai"
       version = "0.1.0"
       description = "CONNECT.AI — motor de matchmaking semântico multi-agente (CP5 FIAP)"
       readme = "README.md"
       requires-python = ">=3.10"
       authors = [
           { name = "Matheus Barbosa", email = "rm561085@fiap.com.br" },
           { name = "Guilherme Henrique", email = "rm559977@fiap.com.br" },
           { name = "Vitor Adauto", email = "rm560247@fiap.com.br" },
       ]
       dependencies = [
           "langgraph>=0.2.0",
           "chromadb>=0.5.0",
           "google-generativeai>=0.8.0",
           "streamlit>=1.36.0",
           "pandas>=2.2.0",
           "python-dotenv>=1.0.0",
           "pydantic>=2.6.0",
       ]

       [project.optional-dependencies]
       dev = ["pytest>=8.0.0"]

       [tool.setuptools.packages.find]
       where = ["."]
       include = ["connect_ai*"]
       exclude = ["app*", "tests*", "notebook*", "relatorio*"]
       ```

       Observação: o pacote `app/` NÃO entra no `connect_ai` instalado — ele é executado via `streamlit run app/streamlit_app.py`, sem necessidade de import como pacote.

    3. `.env.example` — documentar todas as variáveis necessárias com comentários PT-BR:
       ```
       # Chave de API do Google AI Studio (Gemini Pro + text-embedding-004)
       # Obtenha em: https://aistudio.google.com/app/apikey
       # NUNCA commitar este arquivo preenchido. Copie para `.env` (gitignored) e preencha localmente.
       GOOGLE_API_KEY=cole-sua-chave-aqui

       # Diretório onde o ChromaDB persistira a coleção (deve estar no .gitignore)
       # Padrao: ./chroma_db
       CHROMA_PERSIST_DIR=./chroma_db

       # Nome da colecao do ChromaDB
       # Padrao: perfis_connect_ai
       CHROMA_COLLECTION=perfis_connect_ai
       ```

    NÃO use `setup.py` — `pyproject.toml` é a forma moderna e suficiente. NÃO adicione `chromadb-client` (é uma dependência separada para o modo cliente-servidor; usaremos o modo embedded padrão).
  </action>
  <verify>
    <automated>test -f requirements.txt && test -f pyproject.toml && test -f .env.example && grep -q "langgraph" requirements.txt && grep -q "chromadb" requirements.txt && grep -q "google-generativeai" requirements.txt && grep -q "streamlit" requirements.txt && grep -q "pandas" requirements.txt && grep -q "python-dotenv" requirements.txt && grep -q "pytest" requirements.txt && grep -q "name = \"connect_ai\"" pyproject.toml && grep -q "GOOGLE_API_KEY" .env.example && grep -q "CHROMA_PERSIST_DIR" .env.example</automated>
  </verify>
  <acceptance_criteria>
    - `requirements.txt` lista todas as dependências obrigatórias: `langgraph`, `chromadb`, `google-generativeai`, `streamlit`, `pandas`, `python-dotenv`, `pydantic`, `pytest`
    - `pyproject.toml` contém `name = "connect_ai"`, `version = "0.1.0"`, `requires-python = ">=3.10"` e seção `[tool.setuptools.packages.find]` que inclui `connect_ai*`
    - `pyproject.toml` declara as mesmas dependências de produção que `requirements.txt` (exceto `pytest`, que vai em `[project.optional-dependencies] dev`)
    - `.env.example` contém `GOOGLE_API_KEY=`, `CHROMA_PERSIST_DIR=`, `CHROMA_COLLECTION=`
    - Comando `python -m pip install -e . --dry-run` (ou execução real `pip install -e .`) exibe `Successfully installed connect_ai-0.1.0` ou equivalente sem erros
    - Após instalação, `python -c "import connect_ai; print(connect_ai.__version__)"` imprime `0.1.0` sem precisar de `sys.path` manual
  </acceptance_criteria>
  <done>Pacote pode ser instalado em modo editável e dependências estão declaradas em ambos os formatos (`requirements.txt` para `pip install -r` e `pyproject.toml` para `pip install -e .`).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implementar módulo de configuração com leitura segura de chaves</name>
  <files>connect_ai/config.py, tests/test_config.py</files>
  <behavior>
    - `obter_chave_api("GOOGLE_API_KEY")` retorna a string da variável quando ela está definida no ambiente
    - `obter_chave_api("VAR_INEXISTENTE")` levanta `ConfigError` (subclasse de `RuntimeError`) com mensagem em PT-BR contendo o nome da variável faltante e instruções para configurar o `.env`
    - `obter_chave_api` aceita parâmetro opcional `padrao` que é retornado quando a variável não existe (sem levantar erro)
    - `carregar_env()` invoca `dotenv.load_dotenv()` e é idempotente (chamar 2x não causa erro)
    - `obter_diretorio_chroma()` retorna o valor de `CHROMA_PERSIST_DIR` ou o padrão `"./chroma_db"`
    - `obter_nome_colecao()` retorna o valor de `CHROMA_COLLECTION` ou o padrão `"perfis_connect_ai"`
  </behavior>
  <action>
    Criar dois arquivos. Comece pelos testes (RED), depois a implementação (GREEN).

    1. `tests/test_config.py` — escrever PRIMEIRO, deve falhar inicialmente:
       ```python
       """Testes do modulo de configuracao do CONNECT.AI."""

       import os

       import pytest

       from connect_ai.config import (
           ConfigError,
           carregar_env,
           obter_chave_api,
           obter_diretorio_chroma,
           obter_nome_colecao,
       )


       def test_obter_chave_api_retorna_valor_quando_definida(monkeypatch):
           monkeypatch.setenv("GOOGLE_API_KEY", "chave-fake-123")
           assert obter_chave_api("GOOGLE_API_KEY") == "chave-fake-123"


       def test_obter_chave_api_levanta_erro_ptbr_quando_ausente(monkeypatch):
           monkeypatch.delenv("VAR_QUE_NAO_EXISTE", raising=False)
           with pytest.raises(ConfigError) as exc:
               obter_chave_api("VAR_QUE_NAO_EXISTE")
           mensagem = str(exc.value)
           # Mensagem deve estar em PT-BR e citar a variavel ausente
           assert "VAR_QUE_NAO_EXISTE" in mensagem
           assert ".env" in mensagem
           # Deve conter ao menos uma palavra inequivocamente PT-BR
           assert any(t in mensagem.lower() for t in ["nao", "configur", "ausente", "definida", "faltando"])


       def test_obter_chave_api_aceita_padrao(monkeypatch):
           monkeypatch.delenv("VAR_OPCIONAL", raising=False)
           assert obter_chave_api("VAR_OPCIONAL", padrao="fallback") == "fallback"


       def test_carregar_env_e_idempotente(tmp_path, monkeypatch):
           # Nao deve levantar erro mesmo sem .env presente, nem ao chamar 2x
           monkeypatch.chdir(tmp_path)
           carregar_env()
           carregar_env()


       def test_obter_diretorio_chroma_padrao(monkeypatch):
           monkeypatch.delenv("CHROMA_PERSIST_DIR", raising=False)
           assert obter_diretorio_chroma() == "./chroma_db"


       def test_obter_diretorio_chroma_customizado(monkeypatch):
           monkeypatch.setenv("CHROMA_PERSIST_DIR", "/tmp/meu_chroma")
           assert obter_diretorio_chroma() == "/tmp/meu_chroma"


       def test_obter_nome_colecao_padrao(monkeypatch):
           monkeypatch.delenv("CHROMA_COLLECTION", raising=False)
           assert obter_nome_colecao() == "perfis_connect_ai"


       def test_config_error_e_runtime_error():
           assert issubclass(ConfigError, RuntimeError)
       ```

    2. `connect_ai/config.py` — implementação que faz os testes acima passarem:
       ```python
       """Modulo de configuracao do CONNECT.AI.

       Centraliza a leitura de variaveis de ambiente e chaves de API.
       Por decisao de projeto (PROJECT.md), credenciais sao lidas exclusivamente
       via `.env` ou variaveis de ambiente -- nunca hardcoded.
       """

       from __future__ import annotations

       import os
       from typing import Optional

       from dotenv import load_dotenv


       class ConfigError(RuntimeError):
           """Erro de configuracao do CONNECT.AI (chave ausente, .env mal formado, etc.)."""


       _ENV_CARREGADO = False


       def carregar_env() -> None:
           """Carrega o arquivo `.env` da raiz do projeto.

           Idempotente: chamar varias vezes nao causa erro nem reprocessa o arquivo.
           Se o `.env` nao existir, segue silenciosamente (variaveis podem vir do
           ambiente do sistema).
           """
           global _ENV_CARREGADO
           if _ENV_CARREGADO:
               return
           load_dotenv()
           _ENV_CARREGADO = True


       def obter_chave_api(nome: str, padrao: Optional[str] = None) -> str:
           """Le uma variavel de ambiente, com mensagem de erro clara em PT-BR.

           Args:
               nome: Nome da variavel de ambiente (ex: "GOOGLE_API_KEY").
               padrao: Valor a retornar se a variavel nao existir. Quando None
                   (default) e a variavel estiver ausente, levanta ConfigError.

           Returns:
               O valor da variavel de ambiente, ou `padrao` se fornecido.

           Raises:
               ConfigError: Se a variavel nao existir e `padrao` for None.
           """
           carregar_env()
           valor = os.environ.get(nome)
           if valor is not None and valor != "":
               return valor
           if padrao is not None:
               return padrao
           raise ConfigError(
               f"A variavel de ambiente '{nome}' nao esta definida. "
               f"Configure-a no arquivo .env (copie .env.example para .env e "
               f"preencha o valor) ou exporte-a no terminal antes de executar a aplicacao."
           )


       def obter_diretorio_chroma() -> str:
           """Retorna o diretorio de persistencia do ChromaDB.

           Le `CHROMA_PERSIST_DIR` do ambiente; padrao = './chroma_db'.
           """
           return obter_chave_api("CHROMA_PERSIST_DIR", padrao="./chroma_db")


       def obter_nome_colecao() -> str:
           """Retorna o nome da colecao do ChromaDB.

           Le `CHROMA_COLLECTION` do ambiente; padrao = 'perfis_connect_ai'.
           """
           return obter_chave_api("CHROMA_COLLECTION", padrao="perfis_connect_ai")
       ```

    Ordem de execucao TDD:
    1. Criar `tests/test_config.py`.
    2. Rodar `pytest tests/test_config.py` -> deve falhar com `ImportError`.
    3. Criar `connect_ai/config.py`.
    4. Rodar `pytest tests/test_config.py -v` -> 8 testes devem passar.
  </action>
  <verify>
    <automated>python -m pytest tests/test_config.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `connect_ai/config.py` existe e exporta: `ConfigError`, `carregar_env`, `obter_chave_api`, `obter_diretorio_chroma`, `obter_nome_colecao`
    - `ConfigError` é subclasse de `RuntimeError` (verificável por `python -c "from connect_ai.config import ConfigError; assert issubclass(ConfigError, RuntimeError)"`)
    - Comando `pytest tests/test_config.py -v` reporta `8 passed` (zero falhas, zero erros)
    - Comando `python -c "import os; os.environ.pop('GOOGLE_API_KEY', None); from connect_ai.config import obter_chave_api; obter_chave_api('GOOGLE_API_KEY')"` falha com `ConfigError` cuja mensagem contém `GOOGLE_API_KEY` e `.env` (não com `KeyError`)
    - Nenhuma string `os.environ[` aparece sem fallback no `connect_ai/config.py` (use `os.environ.get` para permitir mensagem customizada)
  </acceptance_criteria>
  <done>Módulo de configuração centraliza leitura de chaves de API com erro PT-BR claro; testes unitários passam.</done>
</task>

</tasks>

<verification>
- `pip install -e .` executado a partir da raiz do projeto retorna sucesso
- `python -c "from connect_ai.config import obter_chave_api"` não levanta erro
- `pytest tests/test_config.py` reporta 8 testes passando
- `git status --ignored` mostra `.env` (se existir) e `chroma_db/` (se existir) na lista de ignorados
- `grep -r "AIza" connect_ai/ app/ tests/ 2>/dev/null` não retorna nenhum hit (zero credenciais hardcoded)
</verification>

<success_criteria>
- Pacote `connect_ai` é importável após `pip install -e .`
- Estrutura de diretórios completa: `connect_ai/`, `app/`, `notebook/`, `relatorio/`, `tests/`
- Todas as dependências do BRIEFING §5 estão declaradas em `requirements.txt` e `pyproject.toml`
- `.env.example` documenta `GOOGLE_API_KEY`, `CHROMA_PERSIST_DIR`, `CHROMA_COLLECTION`
- `.gitignore` previne commit de `.env` e `chroma_db/`
- Leitura de chave ausente produz `ConfigError` com mensagem PT-BR (não `KeyError`)
- Testes do módulo de configuração passam (8/8)
</success_criteria>

<output>
Após completar todas as tasks, criar `.planning/phases/01-fundacao/01-01-SUMMARY.md` com:
- Lista de arquivos criados
- Versões das dependências escolhidas
- Comando de instalação validado
- Decisões tomadas (build-backend setuptools vs hatch, escopo do `[tool.setuptools.packages.find]`, etc.)
</output>
