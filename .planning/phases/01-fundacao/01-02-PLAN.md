---
phase: 01-fundacao
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - connect_ai/schema.py
  - tests/test_schema.py
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04

must_haves:
  truths:
    - "Um perfil sintético pode ser instanciado com os 3 grupos de campos (estruturados, semânticos, multimodais) + `personalidade_ia`"
    - "Idade fora de [18, 99] é rejeitada na validação com erro Pydantic em PT-BR"
    - "Gênero fora do conjunto aceito é rejeitado"
    - "Objetivo fora de {namoro, casual, amizade} é rejeitado"
    - "`gerar_uuid()` retorna strings UUIDv4 válidas e únicas a cada chamada"
    - "`construir_documento_semantico(perfil)` concatena bio + interesses + objetivo + personalidade_ia em um texto não vazio"
  artifacts:
    - path: "connect_ai/schema.py"
      provides: "Modelo Pydantic `Perfil`, função `gerar_uuid()` e função `construir_documento_semantico(perfil)`"
      contains: "class Perfil"
    - path: "tests/test_schema.py"
      provides: "Testes unitários cobrindo validação, UUID e construção do documento"
      contains: "def test_"
  key_links:
    - from: "connect_ai/schema.py (construir_documento_semantico)"
      to: "Perfil.bio + Perfil.interesses + Perfil.objetivo + Perfil.personalidade_ia"
      via: "concatenação textual em formato definido"
      pattern: "perfil\\.(bio|interesses|objetivo|personalidade_ia)"
    - from: "Perfil.objetivo (validator)"
      to: "{namoro, casual, amizade}"
      via: "Pydantic Literal ou validator customizado"
      pattern: "namoro|casual|amizade"
---

<objective>
Definir o schema de dados central do CONNECT.AI: o modelo Pydantic `Perfil` com os 3 grupos de campos do BRIEFING §6 (estruturados, semânticos, multimodais) + o campo gerado `personalidade_ia`. Implementar utilitários de UUID e a função canônica que monta o "documento semântico" a ser embedado pelo `text-embedding-004`.

Propósito: Este schema é a fonte da verdade compartilhada entre TODAS as fases seguintes — seed data (Fase 2), agentes (Fase 3), pipeline de ingestão (Fase 4), pipeline de consumo (Fase 5), front (Fase 6) e notebook (Fase 7) consomem `Perfil`. Mudar este schema depois custa caro.

Saída: Modelo `Perfil` validado, geração de UUID estável, função única para construir o documento que vai ao embedding model.
</objective>

<execution_context>
@C:/Users/power/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/power/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@BRIEFING.md
@.planning/phases/01-fundacao/01-01-PLAN.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implementar modelo Pydantic `Perfil` com validação</name>
  <files>connect_ai/schema.py, tests/test_schema.py</files>
  <behavior>
    - Instanciar `Perfil` com todos os campos válidos retorna um objeto sem erro
    - Campo `idade` aceita inteiros em [18, 99]; rejeita 17 e 100 com `ValidationError`
    - Campo `genero` aceita {"feminino", "masculino", "nao_binario", "outro"}; rejeita "qualquer_string"
    - Campo `genero_preferido` aceita os mesmos valores de `genero` mais "todos"
    - Campo `objetivo` aceita {"namoro", "casual", "amizade"}; rejeita "outro_objetivo"
    - Campo `faixa_etaria_pref` é tupla (int, int) com primeiro <= segundo, ambos em [18, 99]
    - Campo `personalidade_ia` é opcional (default `None` ou string vazia) — preenchido pelo Perfilador depois
    - Campo `interesses` é lista não vazia de strings (pelo menos 1)
    - Campo `bio` é string não vazia (`min_length=1`)
    - Campo `id` é string (UUID); pode ser fornecido ou gerado automaticamente pelo factory
  </behavior>
  <action>
    Implementar dois arquivos. TDD: testes primeiro (RED), depois schema (GREEN).

    1. `tests/test_schema.py` — testes do modelo `Perfil`:
       ```python
       """Testes do schema de dados do CONNECT.AI (Perfil)."""

       import pytest
       from pydantic import ValidationError

       from connect_ai.schema import Perfil


       def _perfil_valido_kwargs(**overrides):
           """Helper: retorna um dict de kwargs validos para instanciar Perfil."""
           base = dict(
               nome="Maria Silva",
               idade=28,
               cidade="Sao Paulo",
               genero="feminino",
               genero_preferido="masculino",
               faixa_etaria_pref=(25, 35),
               objetivo="namoro",
               bio="Adoro caminhadas, jazz e culinaria italiana.",
               interesses=["caminhada", "jazz", "culinaria"],
               foto_perfil=None,
               fotos_extras=[],
           )
           base.update(overrides)
           return base


       def test_perfil_valido_e_instanciado():
           perfil = Perfil(**_perfil_valido_kwargs())
           assert perfil.nome == "Maria Silva"
           assert perfil.idade == 28
           assert perfil.objetivo == "namoro"
           assert perfil.id  # gerado automaticamente


       def test_perfil_aceita_id_explicito():
           perfil = Perfil(**_perfil_valido_kwargs(id="id-fixo-123"))
           assert perfil.id == "id-fixo-123"


       def test_idade_minima_18():
           with pytest.raises(ValidationError):
               Perfil(**_perfil_valido_kwargs(idade=17))


       def test_idade_maxima_99():
           with pytest.raises(ValidationError):
               Perfil(**_perfil_valido_kwargs(idade=100))


       def test_genero_invalido_rejeitado():
           with pytest.raises(ValidationError):
               Perfil(**_perfil_valido_kwargs(genero="invalido"))


       def test_genero_preferido_aceita_todos():
           perfil = Perfil(**_perfil_valido_kwargs(genero_preferido="todos"))
           assert perfil.genero_preferido == "todos"


       def test_objetivo_invalido_rejeitado():
           with pytest.raises(ValidationError):
               Perfil(**_perfil_valido_kwargs(objetivo="trabalho"))


       def test_objetivos_validos_aceitos():
           for obj in ["namoro", "casual", "amizade"]:
               perfil = Perfil(**_perfil_valido_kwargs(objetivo=obj))
               assert perfil.objetivo == obj


       def test_faixa_etaria_invertida_rejeitada():
           with pytest.raises(ValidationError):
               Perfil(**_perfil_valido_kwargs(faixa_etaria_pref=(40, 25)))


       def test_faixa_etaria_fora_de_18_99_rejeitada():
           with pytest.raises(ValidationError):
               Perfil(**_perfil_valido_kwargs(faixa_etaria_pref=(15, 30)))


       def test_interesses_vazio_rejeitado():
           with pytest.raises(ValidationError):
               Perfil(**_perfil_valido_kwargs(interesses=[]))


       def test_bio_vazia_rejeitada():
           with pytest.raises(ValidationError):
               Perfil(**_perfil_valido_kwargs(bio=""))


       def test_personalidade_ia_opcional():
           perfil = Perfil(**_perfil_valido_kwargs())
           # Aceita None ou string vazia como "ainda nao processado pelo Perfilador"
           assert perfil.personalidade_ia in (None, "")


       def test_personalidade_ia_pode_ser_preenchida():
           perfil = Perfil(
               **_perfil_valido_kwargs(personalidade_ia="Introvertida com forte interesse cultural.")
           )
           assert "Introvertida" in perfil.personalidade_ia
       ```

    2. `connect_ai/schema.py` — modelo Pydantic v2:
       ```python
       """Schema de dados do CONNECT.AI.

       Define o modelo `Perfil` com 3 grupos de campos (BRIEFING secao 6):
         - Estruturados: id, nome, idade, cidade, genero, genero_preferido,
           faixa_etaria_pref, objetivo
         - Semanticos: bio, interesses
         - Multimodais: foto_perfil, fotos_extras
         - Gerado: personalidade_ia (preenchido pelo Agente Perfilador)
       """

       from __future__ import annotations

       import uuid
       from typing import List, Literal, Optional, Tuple

       from pydantic import BaseModel, Field, field_validator, model_validator


       Genero = Literal["feminino", "masculino", "nao_binario", "outro"]
       GeneroPreferido = Literal["feminino", "masculino", "nao_binario", "outro", "todos"]
       Objetivo = Literal["namoro", "casual", "amizade"]


       def gerar_uuid() -> str:
           """Gera um identificador UUIDv4 como string. Usado como id padrao do Perfil."""
           return str(uuid.uuid4())


       class Perfil(BaseModel):
           """Modelo de perfil do CONNECT.AI.

           Combina dados estruturados (filtros hard), semanticos (embedding) e
           multimodais (Perfilador). O campo `personalidade_ia` e preenchido
           apos a passagem pelo Agente Perfilador.
           """

           # Estruturados
           id: str = Field(default_factory=gerar_uuid)
           nome: str = Field(min_length=1, max_length=120)
           idade: int = Field(ge=18, le=99)
           cidade: str = Field(min_length=1, max_length=120)
           genero: Genero
           genero_preferido: GeneroPreferido
           faixa_etaria_pref: Tuple[int, int]
           objetivo: Objetivo

           # Semanticos
           bio: str = Field(min_length=1, max_length=2000)
           interesses: List[str] = Field(min_length=1)

           # Multimodais (placeholders -- mocks textuais por decisao de PROJECT.md)
           foto_perfil: Optional[str] = None
           fotos_extras: List[str] = Field(default_factory=list)

           # Gerado pelo Perfilador
           personalidade_ia: Optional[str] = None

           @field_validator("interesses")
           @classmethod
           def _interesses_nao_vazios(cls, v: List[str]) -> List[str]:
               limpos = [s.strip() for s in v if s and s.strip()]
               if not limpos:
                   raise ValueError("A lista de interesses nao pode ser vazia.")
               return limpos

           @model_validator(mode="after")
           def _validar_faixa_etaria(self) -> "Perfil":
               minimo, maximo = self.faixa_etaria_pref
               if not (18 <= minimo <= 99 and 18 <= maximo <= 99):
                   raise ValueError(
                       "faixa_etaria_pref deve conter dois inteiros entre 18 e 99."
                   )
               if minimo > maximo:
                   raise ValueError(
                       "faixa_etaria_pref deve ter o limite inferior <= limite superior."
                   )
               return self
       ```

    Ordem TDD:
    1. Criar `tests/test_schema.py`.
    2. Rodar `pytest tests/test_schema.py` -> falha por `ImportError`.
    3. Criar `connect_ai/schema.py` com o modelo.
    4. Rodar `pytest tests/test_schema.py -v` -> 14 testes passam.
  </action>
  <verify>
    <automated>python -m pytest tests/test_schema.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Arquivo `connect_ai/schema.py` existe e exporta `Perfil`, `gerar_uuid`, `Genero`, `GeneroPreferido`, `Objetivo`
    - `Perfil` é subclasse de `pydantic.BaseModel` (verificável: `python -c "from pydantic import BaseModel; from connect_ai.schema import Perfil; assert issubclass(Perfil, BaseModel)"`)
    - `pytest tests/test_schema.py -v` reporta `14 passed`
    - Comando `python -c "from connect_ai.schema import gerar_uuid; import uuid; uuid.UUID(gerar_uuid())"` executa sem erro (UUID válido)
    - `gerar_uuid()` chamado 100 vezes produz 100 strings distintas (sem colisão)
    - Tentar instanciar `Perfil(idade=15, ...)` levanta `pydantic.ValidationError`
  </acceptance_criteria>
  <done>Modelo `Perfil` é a fonte da verdade do schema; validações cobrem idade, gênero, objetivo, faixa etária e interesses; UUID gerado deterministicamente válido.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implementar `construir_documento_semantico` para o pipeline de embedding</name>
  <files>connect_ai/schema.py, tests/test_schema.py</files>
  <behavior>
    - `construir_documento_semantico(perfil)` retorna uma string não vazia
    - Para um perfil com `bio="Adoro jazz"`, `interesses=["musica", "leitura"]`, `objetivo="namoro"`, `personalidade_ia="Introvertida"`, o resultado contém TODAS as substrings: "Adoro jazz", "musica", "leitura", "namoro", "Introvertida"
    - Quando `personalidade_ia` é `None` ou vazia, a função NÃO inclui o token literal "None" nem "null" no output (degrada graciosamente, omitindo a seção)
    - O resultado é estável: chamar 2x com o mesmo perfil retorna a mesma string
    - O resultado é human-readable em PT-BR (contém marcadores como "Bio:", "Interesses:", "Objetivo:", "Personalidade:")
  </behavior>
  <action>
    Estender `connect_ai/schema.py` com a função e adicionar testes em `tests/test_schema.py`.

    1. Adicionar ao FIM de `connect_ai/schema.py`:
       ```python
       def construir_documento_semantico(perfil: Perfil) -> str:
           """Monta o "documento semantico" a ser embedado pelo text-embedding-004.

           Concatena bio + interesses + objetivo + personalidade_ia em um texto
           PT-BR estruturado. Esta funcao e a fonte unica da verdade do que e
           enviado ao modelo de embeddings -- todos os pipelines (ingestao,
           consumo) DEVEM usa-la para garantir consistencia entre o vetor
           armazenado e o vetor de query.

           Args:
               perfil: Instancia validada de Perfil.

           Returns:
               String em PT-BR no formato:
                 "Bio: <bio>. Interesses: <i1>, <i2>, ... . Objetivo: <obj>. Personalidade: <p>"
               Quando `personalidade_ia` esta vazia, a secao "Personalidade" e omitida.
           """
           partes = [
               f"Bio: {perfil.bio.strip()}.",
               f"Interesses: {', '.join(perfil.interesses)}.",
               f"Objetivo: {perfil.objetivo}.",
           ]
           if perfil.personalidade_ia and perfil.personalidade_ia.strip():
               partes.append(f"Personalidade: {perfil.personalidade_ia.strip()}.")
           return " ".join(partes)
       ```

    2. Adicionar AO FINAL de `tests/test_schema.py`:
       ```python
       from connect_ai.schema import construir_documento_semantico


       def test_documento_inclui_bio_interesses_objetivo():
           perfil = Perfil(**_perfil_valido_kwargs(
               bio="Adoro jazz",
               interesses=["musica", "leitura"],
               objetivo="namoro",
           ))
           doc = construir_documento_semantico(perfil)
           assert "Adoro jazz" in doc
           assert "musica" in doc
           assert "leitura" in doc
           assert "namoro" in doc


       def test_documento_inclui_personalidade_quando_preenchida():
           perfil = Perfil(**_perfil_valido_kwargs(
               personalidade_ia="Introvertida e cultural"
           ))
           doc = construir_documento_semantico(perfil)
           assert "Introvertida e cultural" in doc
           assert "Personalidade" in doc


       def test_documento_omite_personalidade_quando_ausente():
           perfil = Perfil(**_perfil_valido_kwargs(personalidade_ia=None))
           doc = construir_documento_semantico(perfil)
           # Nao deve vazar tokens "None" / "null" no texto enviado ao embedding
           assert "None" not in doc
           assert "null" not in doc
           assert "Personalidade" not in doc


       def test_documento_e_estavel():
           perfil = Perfil(**_perfil_valido_kwargs())
           assert construir_documento_semantico(perfil) == construir_documento_semantico(perfil)


       def test_documento_contem_marcadores_ptbr():
           perfil = Perfil(**_perfil_valido_kwargs())
           doc = construir_documento_semantico(perfil)
           assert "Bio:" in doc
           assert "Interesses:" in doc
           assert "Objetivo:" in doc
       ```

    Ordem TDD:
    1. Adicionar os 5 novos testes em `tests/test_schema.py`.
    2. Rodar `pytest tests/test_schema.py -k documento` -> falha (funcao nao existe).
    3. Adicionar `construir_documento_semantico` em `connect_ai/schema.py`.
    4. Rodar `pytest tests/test_schema.py -v` -> 19 testes passam (14 da task anterior + 5 novos).
  </action>
  <verify>
    <automated>python -m pytest tests/test_schema.py -v --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - Função `construir_documento_semantico` é exportada de `connect_ai/schema.py` (verificável: `python -c "from connect_ai.schema import construir_documento_semantico"`)
    - Assinatura aceita um `Perfil` e retorna `str` (verificável por inspeção de tipo: `import inspect; sig = inspect.signature(construir_documento_semantico)`)
    - `pytest tests/test_schema.py -v` reporta `19 passed` (14 da Task 1 + 5 desta task)
    - Para um perfil sem `personalidade_ia`, a string `"None"` NUNCA aparece no documento gerado
    - Para um perfil com `personalidade_ia` preenchida, o documento contém o marcador `"Personalidade:"` e o conteúdo
    - Comando `python -c "from connect_ai.schema import Perfil, construir_documento_semantico; p = Perfil(nome='X', idade=25, cidade='SP', genero='feminino', genero_preferido='todos', faixa_etaria_pref=(20,30), objetivo='namoro', bio='hello', interesses=['a']); print(construir_documento_semantico(p))"` imprime uma string não vazia começando com `Bio: hello.`
  </acceptance_criteria>
  <done>Função canônica de construção do documento semântico está implementada, testada e cobre o caso degenerado de `personalidade_ia` ausente sem vazar tokens Python.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_schema.py -v` reporta 19 testes passando
- `python -c "from connect_ai.schema import Perfil, gerar_uuid, construir_documento_semantico"` executa sem erro
- Pelo menos um caso negativo de cada validador (idade, gênero, objetivo, faixa etária, interesses) é coberto pelos testes
- A função `construir_documento_semantico` é determinística (mesmo input → mesmo output)
</verification>

<success_criteria>
- Modelo `Perfil` instanciável com os 3 grupos de campos do BRIEFING §6
- Validações Pydantic bloqueiam: idade fora [18,99], gênero inválido, objetivo inválido, faixa etária invertida, interesses vazios, bio vazia
- `gerar_uuid()` produz UUIDs v4 únicos
- `construir_documento_semantico(perfil)` produz string PT-BR estável e human-readable
- Testes unitários cobrem casos positivos E negativos (19 testes total)
</success_criteria>

<output>
Após completar todas as tasks, criar `.planning/phases/01-fundacao/01-02-SUMMARY.md` com:
- Definição final do schema (campos, tipos, validadores)
- Formato exato do documento semântico (template usado)
- Decisões: por que `personalidade_ia` é opcional, por que `genero_preferido` aceita "todos"
- Lista de exports (`Perfil`, `gerar_uuid`, `construir_documento_semantico`, etc.)
</output>
