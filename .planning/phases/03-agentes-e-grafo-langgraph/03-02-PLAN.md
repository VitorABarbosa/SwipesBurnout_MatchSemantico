---
phase: 03-agentes-e-grafo-langgraph
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - connect_ai/agentes.py
autonomous: true
requirements: [AGT-01, AGT-02, AGT-03, AGT-04, AGT-07]

must_haves:
  truths:
    - "AgentState e um TypedDict com 5 campos: perfil, candidatos, matches, justificativas, erro"
    - "agente_perfilador retorna personalidade_ia deterministico para o mesmo perfil id"
    - "agente_casamenteiro popula matches com dicts que contem id (str) e score (float >= 85.0)"
    - "agente_rag_justificador popula justificativas com strings PT-BR para cada match"
    - "Pipeline Perfilador -> Casamenteiro -> RAG acumula todos os campos corretamente"
  artifacts:
    - path: "connect_ai/agentes.py"
      provides: "AgentState TypedDict + tres funcoes de agente"
      exports: ["AgentState", "agente_perfilador", "agente_casamenteiro", "agente_rag_justificador"]
      min_lines: 120
  key_links:
    - from: "connect_ai/agentes.py"
      to: "connect_ai/schema.py"
      via: "from connect_ai.schema import Perfil"
      pattern: "from connect_ai\\.schema import"
    - from: "connect_ai/agentes.py"
      to: "connect_ai/seed_data.py"
      via: "from connect_ai.seed_data import gerar_pool_perfis"
      pattern: "from connect_ai\\.seed_data import"
---

<objective>
Fase GREEN para os tres agentes: implementa `connect_ai/agentes.py` fazendo todos os testes de `tests/test_agentes.py` passar.

Purpose: Entregar os contratos dos agentes (AgentState + 3 funcoes) que o grafo LangGraph (plano 03-03) ira consumir.
Output: `connect_ai/agentes.py` com AgentState TypedDict, mock deterministico do Perfilador, stub do Casamenteiro e mock do RAG Justificador.
</objective>

<execution_context>
@C:/Users/power/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/power/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@connect_ai/schema.py
@connect_ai/seed_data.py
@connect_ai/scoring_utils.py
@.planning/phases/03-agentes-e-grafo-langgraph/03-01-SUMMARY.md
</context>

<interfaces>
<!-- Contratos que o executor precisa respeitar. Extraidos do codebase. -->

De connect_ai/schema.py:
```python
class Perfil(BaseModel):
    id: str                          # chave para cache do Perfilador
    nome: str
    idade: int
    cidade: str
    genero: Genero                   # Literal["feminino","masculino","nao_binario","outro"]
    genero_preferido: GeneroPreferido
    faixa_etaria_pref: Tuple[int, int]
    objetivo: Objetivo               # Literal["namoro","casual","amizade"]
    bio: str
    interesses: List[str]
    foto_perfil: Optional[str]
    fotos_extras: List[str]
    personalidade_ia: Optional[str]  # None antes do Perfilador, str depois
```

De connect_ai/seed_data.py:
```python
PERFIL_TESTE: Perfil   # id="perfil-teste-ana-lima-001", nome="Ana Lima", idade=27
def gerar_pool_perfis() -> List[Perfil]:  # 100 perfis, 20 alta-compat + 80 diversidade
```

Contratos esperados pelos testes (03-01-PLAN.md):
```python
# AgentState deve ser TypedDict com exatamente estes campos
class AgentState(TypedDict):
    perfil: Perfil
    candidatos: List[Dict]
    matches: List[Dict]
    justificativas: Dict[str, str]
    erro: Optional[str]

# Cada agente recebe e retorna AgentState
def agente_perfilador(state: AgentState) -> AgentState: ...
def agente_casamenteiro(state: AgentState) -> AgentState: ...
def agente_rag_justificador(state: AgentState) -> AgentState: ...
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Implementar connect_ai/agentes.py (GREEN)</name>
  <files>connect_ai/agentes.py</files>

  <read_first>
    - tests/test_agentes.py — LEITURA OBRIGATORIA antes de escrever qualquer linha de codigo. Cada teste define um contrato preciso.
    - connect_ai/schema.py — campos exatos de Perfil (especialmente id, interesses, objetivo, personalidade_ia)
    - connect_ai/seed_data.py — gerar_pool_perfis() retorna List[Perfil]; PERFIL_TESTE.id = "perfil-teste-ana-lima-001"
    - .planning/phases/03-agentes-e-grafo-langgraph/03-01-SUMMARY.md — decisoes tomadas na fase RED
  </read_first>

  <behavior>
    - AgentState: TypedDict com campos perfil (Perfil), candidatos (List[Dict]), matches (List[Dict]), justificativas (Dict[str, str]), erro (Optional[str])
    - agente_perfilador: LRU cache keyed no perfil.id; retorna copia do Perfil com personalidade_ia preenchida por template deterministico PT-BR baseado em bio+interesses+objetivo; nunca chama API real
    - agente_casamenteiro: popula matches com dicts {"id": str, "score": float, "nome": str} usando _calcular_score_stub que retorna 90.0; usa gerar_pool_perfis() como fonte de candidatos mock
    - agente_rag_justificador: modo mock (sem GOOGLE_API_KEY); para cada match em state["matches"], gera justificativa deterministica PT-BR como string template
    - Todos os agentes: recebem AgentState, retornam AgentState modificado (imutavel — criar novo dict ou usar {**state, campo: novo_valor})
  </behavior>

  <action>
Criar `connect_ai/agentes.py` com a estrutura abaixo. Cada secao deve ser implementada exatamente como descrito.

**Secao 1 — Imports e AgentState:**
```python
"""Agentes LangGraph do CONNECT.AI.

Define AgentState (TypedDict compartilhado) e os tres nos do grafo:
  - agente_perfilador: mock deterministico do Gemini Vision (AGT-02, AGT-07)
  - agente_casamenteiro: stub com scoring fixo, substituivel na Fase 5 (AGT-03)
  - agente_rag_justificador: mock do Gemini Pro, modo real quando API disponivel (AGT-04)

Todos os agentes recebem e devolvem AgentState — contrato exigido pelo LangGraph.
"""
from __future__ import annotations

import functools
import os
from typing import Any, Dict, List, Optional

from typing_extensions import TypedDict

from connect_ai.schema import Perfil


class AgentState(TypedDict):
    """Estado compartilhado entre os nos do grafo LangGraph.

    Campos acumulados ao longo do pipeline:
      perfil: Perfil processado (personalidade_ia preenchida pelo Perfilador).
      candidatos: Top-30 da busca vetorial (populado pelo Casamenteiro, Fase 5).
      matches: Top-10 com score >= 85 (populado pelo Casamenteiro).
      justificativas: Texto PT-BR do RAG para cada match, keyed por id.
      erro: Mensagem de erro se qualquer no falhar; None em execucao normal.
    """
    perfil: Perfil
    candidatos: List[Dict[str, Any]]
    matches: List[Dict[str, Any]]
    justificativas: Dict[str, str]
    erro: Optional[str]
```

**Secao 2 — Agente Perfilador com cache LRU:**
```python
# ── Cache interno do Perfilador (AGT-07: determinismo via cache) ───────────────
_cache_personalidade: Dict[str, str] = {}


def _gerar_personalidade_mock(perfil: Perfil) -> str:
    """Gera descricao de personalidade deterministica baseada nos dados do Perfil.

    Mock textual determinístico: mesmo input -> mesmo output, sem chamar Gemini.
    O texto e suficientemente rico para ser util como entrada do embedding na Fase 4.
    """
    interesses_str = ", ".join(perfil.interesses[:5])
    return (
        f"Perfil de personalidade gerado pelo Perfilador IA (modo mock): "
        f"{perfil.nome} e uma pessoa de {perfil.idade} anos, residente em {perfil.cidade}, "
        f"com objetivo de {perfil.objetivo}. "
        f"Demonstra interesse ativo em: {interesses_str}. "
        f"Bio pessoal: {perfil.bio[:120].strip()}. "
        f"Tracos predominantes: sociavel, comunicativo e orientado a conexoes autenticas."
    )


def agente_perfilador(state: AgentState) -> AgentState:
    """No Perfilador: enriquece o Perfil com descricao de personalidade gerada por IA.

    Usa cache por perfil.id (AGT-07) para garantir determinismo entre chamadas.
    Em producao (Fase futura), chamaria Gemini Vision com temperature=0.
    Nesta implementacao, usa mock textual deterministico.

    Args:
        state: AgentState com campo 'perfil' preenchido.

    Returns:
        AgentState com state['perfil'].personalidade_ia preenchido.
    """
    perfil = state["perfil"]
    id_perfil = perfil.id

    if id_perfil not in _cache_personalidade:
        _cache_personalidade[id_perfil] = _gerar_personalidade_mock(perfil)

    perfil_atualizado = perfil.model_copy(
        update={"personalidade_ia": _cache_personalidade[id_perfil]}
    )
    return {**state, "perfil": perfil_atualizado}
```

**Secao 3 — Stub de scoring e Agente Casamenteiro:**
```python
# ── Stub de scoring (substituido na Fase 5 por connect_ai.scoring) ─────────────

def _calcular_score_stub(candidato: Dict[str, Any], perfil_ref: Perfil) -> float:
    """Stub: retorna 90.0 para todos os candidatos.

    FASE 3 APENAS. A Fase 5 substitui por connect_ai.scoring.calcular_score
    com pesos 60/20/10/5/5 (semantico/interesses/objetivo/idade/geografia).
    """
    return 90.0


def agente_casamenteiro(state: AgentState) -> AgentState:
    """No Casamenteiro: aplica filtros e scoring para selecionar Top-10 matches.

    Fase 3 (stub): usa gerar_pool_perfis() como candidatos mock e _calcular_score_stub.
    Fase 5 substituira a busca vetorial real via Repositorio e scoring ponderado.

    Popula state['matches'] com dicts contendo pelo menos 'id', 'score' e 'nome'.
    Retorna apenas candidatos com score >= 85.0.

    Args:
        state: AgentState com 'perfil' preenchido (apos Perfilador).

    Returns:
        AgentState com 'matches' populado.
    """
    from connect_ai.seed_data import gerar_pool_perfis

    perfil_ref = state["perfil"]
    pool = gerar_pool_perfis()

    matches = []
    for candidato in pool:
        # Filtro hard basico: nao incluir o proprio perfil
        if candidato.id == perfil_ref.id:
            continue
        score = _calcular_score_stub({"id": candidato.id, "nome": candidato.nome}, perfil_ref)
        if score >= 85.0:
            matches.append({
                "id": candidato.id,
                "score": float(score),
                "nome": candidato.nome,
                "cidade": candidato.cidade,
                "idade": candidato.idade,
            })

    # Limitar a 10 matches (Top-10 do BRIEFING)
    matches_top10 = matches[:10]
    return {**state, "matches": matches_top10}
```

**Secao 4 — Agente RAG Justificador:**
```python
def _justificativa_mock(match: Dict[str, Any], perfil_ref: Perfil) -> str:
    """Gera justificativa de compatibilidade deterministica em PT-BR.

    Usado quando GOOGLE_API_KEY nao esta disponivel ou em testes.
    """
    nome_match = match.get("nome", "Candidato")
    score = match.get("score", 90.0)
    return (
        f"{nome_match} apresenta alta compatibilidade com {perfil_ref.nome} "
        f"(score {score:.1f}/100). Compartilham objetivos semelhantes e "
        f"demonstram afinidade semantica elevada baseada em interesses, "
        f"bio e estilo de vida descritos nos perfis."
    )


def agente_rag_justificador(state: AgentState) -> AgentState:
    """No RAG Justificador: gera justificativa textual PT-BR para cada match.

    Modo mock (sem GOOGLE_API_KEY): usa _justificativa_mock deterministico.
    Modo real (com GOOGLE_API_KEY): chamaria Gemini Pro com temperature=0.

    Args:
        state: AgentState com 'matches' preenchido (apos Casamenteiro).

    Returns:
        AgentState com 'justificativas' populado.
    """
    perfil_ref = state["perfil"]
    matches = state["matches"]
    api_key = os.environ.get("GOOGLE_API_KEY", "")
    usar_mock = not api_key

    justificativas: Dict[str, str] = {}

    for match in matches:
        id_match = match["id"]
        if usar_mock:
            justificativas[id_match] = _justificativa_mock(match, perfil_ref)
        else:
            # Modo real: Gemini Pro com temperature=0 (AGT-07)
            # Implementacao completa na Fase futura (RAG com contexto real)
            justificativas[id_match] = _justificativa_mock(match, perfil_ref)

    return {**state, "justificativas": justificativas}
```

Reunir todas as secoes em um unico arquivo `connect_ai/agentes.py` na ordem: imports/TypedDict -> Perfilador -> Casamenteiro -> RAG.

Verificar GREEN:
```bash
cd "/c/Users/power/OneDrive/Documentos/PROJETOS_/Trabalhos/Faculdade/CP5_PLN" && python -m pytest tests/test_agentes.py -v 2>&1 | tail -20
```
Resultado esperado: todos os testes de `test_agentes.py` passando (PASSED).
  </action>

  <verify>
    <automated>cd "/c/Users/power/OneDrive/Documentos/PROJETOS_/Trabalhos/Faculdade/CP5_PLN" && python -m pytest tests/test_agentes.py -v 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `connect_ai/agentes.py` existe com >= 120 linhas
    - `grep -E "^class AgentState" connect_ai/agentes.py` encontra a classe TypedDict
    - `grep -E "^def agente_" connect_ai/agentes.py | wc -l` retorna 3 (tres funcoes de agente)
    - `python -m pytest tests/test_agentes.py -v` mostra todos os testes com status PASSED (nenhum FAILED)
    - `python -m pytest tests/ --ignore=tests/test_grafo.py -q` mostra suite completa passando (sem regressoes)
    - `python -c "from connect_ai.agentes import AgentState, agente_perfilador, agente_casamenteiro, agente_rag_justificador; print('OK')"` imprime OK
  </acceptance_criteria>

  <done>
    - `connect_ai/agentes.py` implementado e todos os testes de `tests/test_agentes.py` passando (GREEN)
    - AgentState TypedDict exportado com 5 campos corretos
    - Tres funcoes de agente exportadas com assinaturas corretas
    - Suite completa sem regressoes
  </done>
</task>

</tasks>

<verification>
Verificacao final do plano 03-02:

```bash
cd "/c/Users/power/OneDrive/Documentos/PROJETOS_/Trabalhos/Faculdade/CP5_PLN" && python -m pytest tests/test_agentes.py -v && python -m pytest tests/ --ignore=tests/test_grafo.py -q 2>&1 | tail -10
```

- Todos os testes de `test_agentes.py` PASSED
- Suite completa sem regressoes
- `connect_ai/grafo.py` NAO deve existir ainda (pertence ao plano 03-03)
</verification>

<success_criteria>
- `connect_ai/agentes.py` criado com AgentState TypedDict + 3 funcoes de agente
- `python -m pytest tests/test_agentes.py` passa 100% (9+ testes PASSED)
- `python -m pytest tests/ --ignore=tests/test_grafo.py` sem regressoes
- Nenhuma chamada real ao Gemini ou ChromaDB (modulo autocontido, testavel offline)
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/03-agentes-e-grafo-langgraph/03-02-SUMMARY.md` com:
- Linhas de codigo de `connect_ai/agentes.py`
- Resultado do `pytest tests/test_agentes.py -v` (quantos passed)
- Decisao sobre o stub `_calcular_score_stub` (como sera substituido na Fase 5)
- Nota sobre o cache `_cache_personalidade` (dict global vs functools.lru_cache)
</output>
