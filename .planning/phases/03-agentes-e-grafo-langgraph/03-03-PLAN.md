---
phase: 03-agentes-e-grafo-langgraph
plan: "03"
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - connect_ai/grafo.py
autonomous: true
requirements: [AGT-05, AGT-06, AGT-07]

must_haves:
  truths:
    - "Grafo LangGraph compila sem erro e tem exatamente 3 nos identificaveis"
    - "pipeline_consumo.invoke() executa os tres agentes em sequencia e retorna state completo"
    - "relatorio/grafo_pipeline.mmd e criado com texto Mermaid contendo os tres nos"
    - "salvar_visualizacao_grafo cria o diretorio de destino se ele nao existir"
  artifacts:
    - path: "connect_ai/grafo.py"
      provides: "StateGraph com 3 nos + funcao de visualizacao"
      exports: ["pipeline_consumo", "salvar_visualizacao_grafo"]
      min_lines: 80
    - path: "relatorio/grafo_pipeline.mmd"
      provides: "Texto Mermaid do grafo (gerado pelo teste de integracao)"
      contains: "perfilador"
  key_links:
    - from: "connect_ai/grafo.py"
      to: "connect_ai/agentes.py"
      via: "from connect_ai.agentes import AgentState, agente_perfilador, agente_casamenteiro, agente_rag_justificador"
      pattern: "from connect_ai\\.agentes import"
    - from: "connect_ai/grafo.py"
      to: "langgraph.graph"
      via: "from langgraph.graph import StateGraph, START, END"
      pattern: "from langgraph\\.graph import"
    - from: "pipeline_consumo"
      to: "agente_perfilador"
      via: "grafo.add_node('perfilador', agente_perfilador)"
      pattern: "add_node.*perfilador"
---

<objective>
Fase GREEN para o grafo LangGraph: implementa `connect_ai/grafo.py` fazendo todos os testes de `tests/test_grafo.py` passar, e salva a visualizacao em `relatorio/`.

Purpose: Montar os tres agentes como nos distintos no StateGraph LangGraph, compilar o grafo e exportar a visualizacao Mermaid/PNG como artefato verificavel do projeto.
Output: `connect_ai/grafo.py` com `pipeline_consumo` (CompiledStateGraph) e `salvar_visualizacao_grafo()`. Arquivo `relatorio/grafo_pipeline.mmd` gerado.
</objective>

<execution_context>
@C:/Users/power/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/power/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@connect_ai/agentes.py
@.planning/phases/03-agentes-e-grafo-langgraph/03-01-SUMMARY.md
@.planning/phases/03-agentes-e-grafo-langgraph/03-02-SUMMARY.md
</context>

<interfaces>
<!-- Contratos necessarios para implementar grafo.py. Extraidos de agentes.py (criado em 03-02). -->

De connect_ai/agentes.py:
```python
class AgentState(TypedDict):
    perfil: Perfil
    candidatos: List[Dict[str, Any]]
    matches: List[Dict[str, Any]]
    justificativas: Dict[str, str]
    erro: Optional[str]

def agente_perfilador(state: AgentState) -> AgentState: ...
def agente_casamenteiro(state: AgentState) -> AgentState: ...
def agente_rag_justificador(state: AgentState) -> AgentState: ...
```

API LangGraph necessaria:
```python
from langgraph.graph import StateGraph, START, END

# Construir grafo
grafo = StateGraph(AgentState)
grafo.add_node("perfilador", agente_perfilador)
grafo.add_node("casamenteiro", agente_casamenteiro)
grafo.add_node("rag_justificador", agente_rag_justificador)
grafo.add_edge(START, "perfilador")
grafo.add_edge("perfilador", "casamenteiro")
grafo.add_edge("casamenteiro", "rag_justificador")
grafo.add_edge("rag_justificador", END)
pipeline_consumo = grafo.compile()

# Visualizacao
mermaid_text: str = pipeline_consumo.get_graph().draw_mermaid()
png_bytes: bytes = pipeline_consumo.get_graph().draw_mermaid_png()
```

Contratos esperados pelos testes (03-01-PLAN.md):
```python
# pipeline_consumo deve ter .invoke e .get_graph()
# pipeline_consumo.get_graph().nodes.keys() contem "perfilador", "casamenteiro", "rag_justificador"
# salvar_visualizacao_grafo(caminho: str = "relatorio/grafo_pipeline.png") -> None
#   - Cria dir se nao existir
#   - Salva .mmd substituindo extensao .png por .mmd
#   - Tenta salvar .png (pode falhar silenciosamente se graphviz ausente)
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Implementar connect_ai/grafo.py (GREEN para test_grafo.py)</name>
  <files>connect_ai/grafo.py</files>

  <read_first>
    - tests/test_grafo.py — LEITURA OBRIGATORIA. Cada teste define o contrato exato de pipeline_consumo e salvar_visualizacao_grafo.
    - connect_ai/agentes.py — AgentState, agente_perfilador, agente_casamenteiro, agente_rag_justificador (criados em 03-02)
    - .planning/phases/03-agentes-e-grafo-langgraph/03-02-SUMMARY.md — confirmar exportacoes de agentes.py
  </read_first>

  <behavior>
    - pipeline_consumo: CompiledStateGraph com 3 nos ("perfilador", "casamenteiro", "rag_justificador") em sequencia START -> perfilador -> casamenteiro -> rag_justificador -> END
    - pipeline_consumo.get_graph().nodes deve conter os tres nomes de no
    - salvar_visualizacao_grafo: cria diretorio se nao existir; salva .mmd (texto Mermaid, sempre); tenta salvar .png (silencia excecao se graphviz/pillow ausente); aceita parametro `caminho` com default "relatorio/grafo_pipeline.png"
    - Arquivo .mmd tem extensao derivada automaticamente: substituir ".png" por ".mmd" no caminho informado
  </behavior>

  <action>
Criar `connect_ai/grafo.py` com o conteudo abaixo:

```python
"""Grafo LangGraph do pipeline de consumo do CONNECT.AI.

Monta os tres agentes como nos distintos de um StateGraph:
  - perfilador    -> agente_perfilador   (AGT-02)
  - casamenteiro  -> agente_casamenteiro (AGT-03)
  - rag_justificador -> agente_rag_justificador (AGT-04)

Fluxo: START -> perfilador -> casamenteiro -> rag_justificador -> END

`pipeline_consumo` e o grafo compilado, pronto para .invoke() e para
ser importado pelo front Streamlit (Fase 6) e pelo notebook (Fase 7).

`salvar_visualizacao_grafo` exporta o diagrama em Mermaid (.mmd) e,
quando as dependencias grafico estao presentes, tambem em PNG.
"""
from __future__ import annotations

import os
from pathlib import Path

from langgraph.graph import END, START, StateGraph

from connect_ai.agentes import (
    AgentState,
    agente_casamenteiro,
    agente_perfilador,
    agente_rag_justificador,
)

# ── Construcao do grafo ───────────────────────────────────────────────────────

_grafo = StateGraph(AgentState)

_grafo.add_node("perfilador", agente_perfilador)
_grafo.add_node("casamenteiro", agente_casamenteiro)
_grafo.add_node("rag_justificador", agente_rag_justificador)

_grafo.add_edge(START, "perfilador")
_grafo.add_edge("perfilador", "casamenteiro")
_grafo.add_edge("casamenteiro", "rag_justificador")
_grafo.add_edge("rag_justificador", END)

# Grafo compilado — importado diretamente pelo front e notebook
pipeline_consumo = _grafo.compile()


# ── Visualizacao ─────────────────────────────────────────────────────────────


def salvar_visualizacao_grafo(caminho: str = "relatorio/grafo_pipeline.png") -> None:
    """Exporta o diagrama do grafo em Mermaid (.mmd) e, se possivel, em PNG.

    O arquivo .mmd e sempre gerado (nao depende de graphviz/pillow).
    O arquivo .png e gerado apenas se as dependencias de renderizacao
    estiverem disponiveis; caso contrario, uma aviso e impresso e a
    funcao retorna normalmente sem lancar excecao.

    O diretorio de destino e criado automaticamente se nao existir.

    Args:
        caminho: Caminho do arquivo PNG de saida.
            Default: "relatorio/grafo_pipeline.png".
            O arquivo .mmd e salvo no mesmo diretorio com extensao trocada.
    """
    caminho_path = Path(caminho)
    caminho_path.parent.mkdir(parents=True, exist_ok=True)

    # Exportar texto Mermaid (sempre disponivel, sem dependencias extras)
    caminho_mmd = caminho_path.with_suffix(".mmd")
    texto_mermaid = pipeline_consumo.get_graph().draw_mermaid()
    caminho_mmd.write_text(texto_mermaid, encoding="utf-8")
    print(f"[grafo] Diagrama Mermaid salvo em: {caminho_mmd}")

    # Tentar exportar PNG (requer graphviz ou pillow instalados)
    try:
        png_bytes = pipeline_consumo.get_graph().draw_mermaid_png()
        caminho_path.write_bytes(png_bytes)
        print(f"[grafo] Diagrama PNG salvo em: {caminho_path}")
    except Exception as excecao:
        print(
            f"[grafo] Aviso: nao foi possivel gerar o PNG ({type(excecao).__name__}: {excecao}). "
            f"O arquivo .mmd foi salvo com sucesso."
        )
```

Salvar como `connect_ai/grafo.py`.

Verificar GREEN:
```bash
cd "/c/Users/power/OneDrive/Documentos/PROJETOS_/Trabalhos/Faculdade/CP5_PLN" && python -m pytest tests/test_grafo.py -v 2>&1 | tail -20
```
Resultado esperado: todos os testes de `test_grafo.py` passando.
  </action>

  <verify>
    <automated>cd "/c/Users/power/OneDrive/Documentos/PROJETOS_/Trabalhos/Faculdade/CP5_PLN" && python -m pytest tests/test_grafo.py -v 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `connect_ai/grafo.py` existe com >= 80 linhas
    - `grep "pipeline_consumo" connect_ai/grafo.py` encontra a variavel
    - `grep "salvar_visualizacao_grafo" connect_ai/grafo.py` encontra a funcao
    - `python -m pytest tests/test_grafo.py -v` mostra todos os testes com status PASSED
    - `python -c "from connect_ai.grafo import pipeline_consumo, salvar_visualizacao_grafo; print('OK')"` imprime OK
    - `python -c "from connect_ai.grafo import pipeline_consumo; nos = set(pipeline_consumo.get_graph().nodes.keys()); assert 'perfilador' in nos and 'casamenteiro' in nos and 'rag_justificador' in nos; print('3 nos OK')"` imprime "3 nos OK"
  </acceptance_criteria>

  <done>
    - `connect_ai/grafo.py` implementado com `pipeline_consumo` e `salvar_visualizacao_grafo`
    - Todos os testes de `test_grafo.py` passando
    - Grafo tem 3 nos distintos verificados por assertion programatica
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: Gerar artefato de visualizacao em relatorio/ e validar suite completa</name>
  <files>relatorio/grafo_pipeline.mmd</files>

  <read_first>
    - connect_ai/grafo.py — implementado na Tarefa 1, confirmar que salvar_visualizacao_grafo funciona
    - tests/test_grafo.py — verificar se o test_mermaid_exportado usa tmp_path; a chamada real vai para relatorio/
  </read_first>

  <action>
Executar a geracao do artefato de visualizacao chamando a funcao diretamente:

```bash
cd "/c/Users/power/OneDrive/Documentos/PROJETOS_/Trabalhos/Faculdade/CP5_PLN" && python -c "
from connect_ai.grafo import salvar_visualizacao_grafo
salvar_visualizacao_grafo()
print('Visualizacao gerada.')
"
```

Isso cria `relatorio/grafo_pipeline.mmd` (e tenta `relatorio/grafo_pipeline.png` se graphviz disponivel).

Verificar o conteudo do .mmd:
```bash
cd "/c/Users/power/OneDrive/Documentos/PROJETOS_/Trabalhos/Faculdade/CP5_PLN" && python -c "
import pathlib
mmd = pathlib.Path('relatorio/grafo_pipeline.mmd').read_text(encoding='utf-8')
assert 'perfilador' in mmd
assert 'casamenteiro' in mmd
assert 'rag_justificador' in mmd
print('MMD valido com os 3 nos.')
print(mmd[:300])
"
```

Em seguida executar a suite completa para confirmar zero regressoes:
```bash
cd "/c/Users/power/OneDrive/Documentos/PROJETOS_/Trabalhos/Faculdade/CP5_PLN" && python -m pytest tests/ -q 2>&1 | tail -10
```
Resultado esperado: todos os testes passando (suite anterior de 51 + novos testes de agentes e grafo).

NAO e necessario ter graphviz instalado para que o criterio de sucesso seja atendido — o arquivo .mmd e suficiente para AGT-06.
  </action>

  <verify>
    <automated>cd "/c/Users/power/OneDrive/Documentos/PROJETOS_/Trabalhos/Faculdade/CP5_PLN" && python -m pytest tests/ -q 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `relatorio/grafo_pipeline.mmd` existe
    - Conteudo do .mmd contem as strings "perfilador", "casamenteiro" e "rag_justificador"
    - `python -m pytest tests/ -q` mostra suite completa passando sem FAILED
    - Numero de testes passando e >= 65 (51 anteriores + 9 de test_agentes + 5 de test_grafo)
    - `relatorio/` diretorio existe (criado automaticamente pela funcao)
  </acceptance_criteria>

  <done>
    - `relatorio/grafo_pipeline.mmd` criado e valido
    - Suite completa de testes sem regressoes
    - Fase 3 tecnicamente concluida: AgentState + 3 agentes + grafo compilado + visualizacao gerada
  </done>
</task>

</tasks>

<verification>
Verificacao final da Fase 3 completa:

```bash
cd "/c/Users/power/OneDrive/Documentos/PROJETOS_/Trabalhos/Faculdade/CP5_PLN" && python -m pytest tests/ -v 2>&1 | grep -E "PASSED|FAILED|ERROR" | tail -30
```

Criterios de sucesso da fase (do ROADMAP.md):
1. O grafo LangGraph e compilado sem erros — verificado por `pipeline_consumo.invoke()`
2. O Perfilador e deterministico — verificado por `test_perfilador_determinismo`
3. Os tres nos sao identificaveis no grafo — verificado por `test_grafo_tem_tres_nos`
4. AgentState acumula dados corretamente — verificado por `test_pipeline_acumulacao`

Verificar todos os arquivos criados na fase:
```bash
cd "/c/Users/power/OneDrive/Documentos/PROJETOS_/Trabalhos/Faculdade/CP5_PLN" && ls connect_ai/agentes.py connect_ai/grafo.py tests/test_agentes.py tests/test_grafo.py relatorio/grafo_pipeline.mmd
```
</verification>

<success_criteria>
- `connect_ai/grafo.py` criado com `pipeline_consumo` (CompiledStateGraph) e `salvar_visualizacao_grafo()`
- `pipeline_consumo.get_graph().nodes` contem "perfilador", "casamenteiro", "rag_justificador"
- `relatorio/grafo_pipeline.mmd` existe com texto Mermaid valido contendo os 3 nos
- `python -m pytest tests/` passa toda a suite (>= 65 testes) sem FAILED
- Arquivos criados na Fase 3: `connect_ai/agentes.py`, `connect_ai/grafo.py`, `tests/test_agentes.py`, `tests/test_grafo.py`, `relatorio/grafo_pipeline.mmd`
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/03-agentes-e-grafo-langgraph/03-03-SUMMARY.md` com:
- Lista de todos os arquivos criados na Fase 3 e contagem de linhas
- Output completo do `pytest tests/ -q` (total de testes passando)
- Conteudo do `relatorio/grafo_pipeline.mmd` (primeiras 10 linhas)
- Nota sobre dependencias de graphviz/pillow para o PNG (para a Fase 7 de entregaveis)
- Decisoes que afetam a Fase 4 (Pipeline de Ingestao): stub do Casamenteiro sera substituido, AgentState fica estavel
</output>
