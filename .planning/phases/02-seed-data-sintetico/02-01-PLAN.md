---
phase: 02-seed-data-sintetico
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_seed_data.py
autonomous: true
requirements:
  - SEED-01
  - SEED-02
  - SEED-03
  - SEED-04

must_haves:
  truths:
    - "O arquivo tests/test_seed_data.py existe e todos os testes falham (RED) antes da implementacao"
    - "Existe um teste que verifica reproducibilidade: gerar_pool_perfis() com seed 42 produz exatamente o mesmo pool em duas chamadas consecutivas"
    - "Existe um teste que verifica que PERFIL_TESTE e um objeto Perfil com campos nao-aleatorios e explicitamente documentados"
    - "Existe um teste que verifica que gerar_pool_perfis() retorna lista com >= 100 Perfis validos"
    - "Existe um teste de analise estatica que conta perfis com interesses_em_comum >= 3, mesmo objetivo e faixa etaria compativel com PERFIL_TESTE e asserta >= 10"
    - "Existe um teste de diversidade: cidades unicas >= 5, idades distribuidas entre 18-60, todos os 3 objetivos presentes"
  artifacts:
    - path: "tests/test_seed_data.py"
      provides: "Suite completa de testes TDD para seed_data.py"
      exports: ["test_reproducibilidade_seed_fixa", "test_perfil_teste_e_explicito", "test_pool_tem_minimo_100_perfis", "test_pool_tem_10_compativeis_com_perfil_teste", "test_pool_diversidade"]
  key_links:
    - from: "tests/test_seed_data.py"
      to: "connect_ai/seed_data.py"
      via: "import from connect_ai.seed_data import gerar_pool_perfis, PERFIL_TESTE"
      pattern: "from connect_ai.seed_data import"
    - from: "tests/test_seed_data.py"
      to: "connect_ai/schema.py"
      via: "Perfil usado para type-check e validacao"
      pattern: "from connect_ai.schema import Perfil"
---

<objective>
Escrever a suite completa de testes com TDD para o gerador de seed data, garantindo que todos os testes falhem (RED) antes de qualquer implementacao.

Purpose: TDD garante que a implementacao subsequente (Plano 02) atende exatamente os criterios verificaveis — reproducibilidade, perfil de teste explicito, pool >= 100 perfis e >= 10 perfis compativeis com score estaticamente verificavel.

Output: tests/test_seed_data.py com 5 funcoes de teste cobrindo SEED-01..04, todos falhando com ImportError ou AssertionError antes da implementacao.
</objective>

<execution_context>
@C:/Users/power/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/power/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@connect_ai/schema.py
@tests/test_schema.py
</context>

<interfaces>
<!-- Contratos que os testes devem importar de connect_ai/seed_data.py (ainda nao existe) -->
<!-- O executor escreve os testes CONTRA estas interfaces esperadas -->

Interface esperada de connect_ai/seed_data.py:
```python
SEED_FIXA: int = 42  # constante inteira

PERFIL_TESTE: Perfil  # instancia explicita, nao-aleatoria, documentada

def gerar_pool_perfis(seed: int = SEED_FIXA) -> list[Perfil]:
    """Gera pool de >= 100 perfis sinteticos de forma reproduzivel."""
    ...
```

Tipos disponiveis em connect_ai/schema.py:
```python
Perfil  # BaseModel com campos: id, nome, idade, cidade, genero,
         # genero_preferido, faixa_etaria_pref (Tuple[int,int]),
         # objetivo, bio, interesses (List[str]), personalidade_ia
Objetivo  # Literal["namoro", "casual", "amizade"]
```

Funcao auxiliar de analise estatica a ser usada nos testes:
```python
def _interesses_em_comum(a: list[str], b: list[str]) -> int:
    return len(set(a) & set(b))

def _faixa_etaria_compativel(idade: int, faixa: tuple[int, int]) -> bool:
    return faixa[0] <= idade <= faixa[1]
```
</interfaces>

<feature>
  <name>Suite TDD: tests/test_seed_data.py</name>
  <files>tests/test_seed_data.py</files>
  <behavior>
    - test_reproducibilidade_seed_fixa: duas chamadas gerar_pool_perfis(seed=42) retornam listas cujos ids (ou nomes) sao identicos posicao a posicao
    - test_perfil_teste_e_explicito: PERFIL_TESTE.nome == "Ana Lima" (string fixa), PERFIL_TESTE.objetivo == "namoro", PERFIL_TESTE.idade == 27, PERFIL_TESTE.interesses contem pelo menos ["musica", "viagem", "fotografia", "yoga", "cinema"] (5 interesses)
    - test_pool_tem_minimo_100_perfis: len(gerar_pool_perfis()) >= 100 e todos os elementos sao instancias de Perfil
    - test_pool_tem_10_compativeis_com_perfil_teste: filtra perfis com interesses_em_comum(p.interesses, PERFIL_TESTE.interesses) >= 3 AND p.objetivo == PERFIL_TESTE.objetivo AND faixa_etaria_compativel(PERFIL_TESTE.idade, p.faixa_etaria_pref) => assert len(compativeis) >= 10
    - test_pool_diversidade: len({p.cidade for p in pool}) >= 5, len({p.objetivo for p in pool}) == 3 (todos os objetivos presentes), qualquer(p.idade <= 28 for p in pool) AND qualquer(p.idade >= 40 for p in pool)
  </behavior>
  <implementation>
    Todos os 5 testes devem ser escritos como funcoes pytest normais (sem parametrizacao complexa).
    Os testes devem falhar com ImportError (connect_ai.seed_data ainda nao existe) antes da implementacao.
    Nao mockar nada — os testes sao de logica pura (nao dependem de ChromaDB, Gemini ou rede).
  </implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Escrever tests/test_seed_data.py (fase RED)</name>
  <files>tests/test_seed_data.py</files>
  <read_first>
    - connect_ai/schema.py (para conhecer a assinatura exata de Perfil e os campos disponiveis)
    - tests/test_schema.py (para replicar o padrao de estilo de testes do projeto: pytest puro, helper _kwargs, docstrings PT-BR)
    - tests/conftest.py (para verificar se ha fixtures compartilhadas a reutilizar)
  </read_first>
  <behavior>
    - test_reproducibilidade_seed_fixa: duas chamadas com seed=42 produzem lista com mesmos nomes na mesma posicao
    - test_perfil_teste_e_explicito: PERFIL_TESTE.nome == "Ana Lima", .objetivo == "namoro", .idade == 27, len(.interesses) >= 5, "musica" in .interesses, "viagem" in .interesses
    - test_pool_tem_minimo_100_perfis: len(pool) >= 100 e isinstance(pool[0], Perfil)
    - test_pool_tem_10_compativeis_com_perfil_teste: >= 10 perfis com interesses_em_comum >= 3 AND mesmo objetivo AND faixa_etaria_pref cobre PERFIL_TESTE.idade
    - test_pool_diversidade: cidades unicas >= 5, os 3 objetivos presentes, idades cobrindo faixa 18-60
  </behavior>
  <action>
Criar tests/test_seed_data.py com o seguinte conteudo exato:

```
"""Testes TDD do gerador de seed data do CONNECT.AI.

Cobre SEED-01 (diversidade), SEED-02 (>= 10 compativeis), SEED-03 (perfil de
teste explicito), SEED-04 (reproducibilidade com seed fixa).

Estes testes sao escritos ANTES da implementacao (fase RED do TDD).
Todos devem falhar com ImportError ate que connect_ai/seed_data.py exista.
"""

from connect_ai.schema import Perfil
from connect_ai.seed_data import PERFIL_TESTE, SEED_FIXA, gerar_pool_perfis


# ── helpers de analise estatica ────────────────────────────────────────────────

def _interesses_em_comum(a: list, b: list) -> int:
    """Conta interesses identicos (case-sensitive) entre duas listas."""
    return len(set(a) & set(b))


def _faixa_etaria_compativel(idade: int, faixa: tuple) -> bool:
    """Retorna True se `idade` esta dentro de `faixa` (inclusive)."""
    return faixa[0] <= idade <= faixa[1]


# ── SEED-04: reproducibilidade ─────────────────────────────────────────────────

def test_reproducibilidade_seed_fixa():
    """Duas chamadas com seed=42 devem produzir o mesmo pool (SEED-04)."""
    pool_a = gerar_pool_perfis(seed=42)
    pool_b = gerar_pool_perfis(seed=42)
    nomes_a = [p.nome for p in pool_a]
    nomes_b = [p.nome for p in pool_b]
    assert nomes_a == nomes_b, (
        "gerar_pool_perfis(seed=42) deve retornar perfis com mesmos nomes "
        "e mesma ordem em execucoes distintas."
    )


# ── SEED-03: perfil de teste explicito ─────────────────────────────────────────

def test_perfil_teste_e_explicito():
    """PERFIL_TESTE deve ter campos fixos e documentados, nao aleatorios (SEED-03).

    Campos verificados: nome 'Ana Lima', objetivo 'namoro', idade 27,
    pelo menos 5 interesses incluindo 'musica' e 'viagem'.
    """
    assert isinstance(PERFIL_TESTE, Perfil), "PERFIL_TESTE deve ser uma instancia de Perfil."
    assert PERFIL_TESTE.nome == "Ana Lima", f"Esperado 'Ana Lima', obtido '{PERFIL_TESTE.nome}'"
    assert PERFIL_TESTE.objetivo == "namoro", (
        f"Esperado objetivo 'namoro', obtido '{PERFIL_TESTE.objetivo}'"
    )
    assert PERFIL_TESTE.idade == 27, f"Esperada idade 27, obtida {PERFIL_TESTE.idade}"
    assert len(PERFIL_TESTE.interesses) >= 5, (
        f"PERFIL_TESTE deve ter >= 5 interesses, tem {len(PERFIL_TESTE.interesses)}"
    )
    assert "musica" in PERFIL_TESTE.interesses, "'musica' deve estar nos interesses do PERFIL_TESTE"
    assert "viagem" in PERFIL_TESTE.interesses, "'viagem' deve estar nos interesses do PERFIL_TESTE"


# ── SEED-01: pool com tamanho e validade ───────────────────────────────────────

def test_pool_tem_minimo_100_perfis():
    """Pool deve ter >= 100 perfis, todos instancias validas de Perfil (SEED-01)."""
    pool = gerar_pool_perfis()
    assert len(pool) >= 100, f"Pool deve ter >= 100 perfis, tem {len(pool)}"
    assert all(isinstance(p, Perfil) for p in pool), (
        "Todos os elementos do pool devem ser instancias de Perfil."
    )


# ── SEED-02: garantia matematica de >= 10 compativeis ─────────────────────────

def test_pool_tem_10_compativeis_com_perfil_teste():
    """Analise estatica: >= 10 perfis compativeis com PERFIL_TESTE (SEED-02).

    Criterios (sem ChromaDB, analise puramente estrutural):
      - interesses_em_comum(p.interesses, PERFIL_TESTE.interesses) >= 3
      - p.objetivo == PERFIL_TESTE.objetivo
      - faixa_etaria_pref de p cobre a idade de PERFIL_TESTE (27)
    """
    pool = gerar_pool_perfis()
    compativeis = [
        p for p in pool
        if (
            _interesses_em_comum(p.interesses, PERFIL_TESTE.interesses) >= 3
            and p.objetivo == PERFIL_TESTE.objetivo
            and _faixa_etaria_compativel(PERFIL_TESTE.idade, p.faixa_etaria_pref)
        )
    ]
    assert len(compativeis) >= 10, (
        f"Pool deve ter >= 10 perfis compativeis com PERFIL_TESTE, "
        f"encontrados {len(compativeis)}. "
        f"Calibre os perfis de alta compatibilidade no seed_data.py."
    )


# ── SEED-01: diversidade do pool ──────────────────────────────────────────────

def test_pool_diversidade():
    """Pool deve cobrir diversidade de cidade, objetivo e faixa etaria (SEED-01)."""
    pool = gerar_pool_perfis()

    cidades_unicas = {p.cidade for p in pool}
    assert len(cidades_unicas) >= 5, (
        f"Pool deve ter >= 5 cidades unicas, tem {len(cidades_unicas)}: {cidades_unicas}"
    )

    objetivos_presentes = {p.objetivo for p in pool}
    assert objetivos_presentes == {"namoro", "casual", "amizade"}, (
        f"Pool deve cobrir os 3 objetivos, encontrados: {objetivos_presentes}"
    )

    idades = [p.idade for p in pool]
    assert min(idades) <= 28, (
        f"Pool deve incluir pessoas jovens (<= 28 anos), minimo encontrado: {min(idades)}"
    )
    assert max(idades) >= 40, (
        f"Pool deve incluir pessoas acima de 40 anos, maximo encontrado: {max(idades)}"
    )
```

O arquivo deve ser criado EXATAMENTE como acima. Nao adicionar mocks, fixtures ou imports extras.
Verificar que SEED_FIXA e importado junto com PERFIL_TESTE e gerar_pool_perfis.
  </action>
  <verify>
    <automated>cd "C:/Users/power/OneDrive/Documentos/PROJETOS_/Trabalhos/Faculdade/CP5_PLN" && python -m pytest tests/test_seed_data.py --tb=short 2>&1 | head -30</automated>
  </verify>
  <done>
    - tests/test_seed_data.py existe
    - O arquivo contem exatamente 5 funcoes de teste: test_reproducibilidade_seed_fixa, test_perfil_teste_e_explicito, test_pool_tem_minimo_100_perfis, test_pool_tem_10_compativeis_com_perfil_teste, test_pool_diversidade
    - Todos os 5 testes FALHAM (fase RED) — esperado "ImportError: cannot import name" ou "ModuleNotFoundError: No module named 'connect_ai.seed_data'" antes da implementacao
    - Verificavel com: grep -c "^def test_" tests/test_seed_data.py retorna 5
  </done>
  <acceptance_criteria>
    - grep "^def test_" tests/test_seed_data.py retorna exatamente 5 linhas
    - grep "from connect_ai.seed_data import PERFIL_TESTE, SEED_FIXA, gerar_pool_perfis" tests/test_seed_data.py retorna 1 linha
    - grep "interesses_em_comum" tests/test_seed_data.py retorna pelo menos 2 linhas (helper + uso)
    - python -m pytest tests/test_seed_data.py 2>&1 | grep -E "ERROR|error|ImportError|ModuleNotFoundError" retorna saida nao-vazia (testes falham como esperado na fase RED)
    - python -m pytest tests/test_seed_data.py 2>&1 | grep "passed" retorna vazio (nenhum teste passou antes da implementacao)
  </acceptance_criteria>
</task>

</tasks>

<verification>
Fase RED verificada quando:
- tests/test_seed_data.py existe com 5 funcoes de teste
- python -m pytest tests/test_seed_data.py retorna falha com ImportError/ModuleNotFoundError
- Nenhum teste passou (0 passed)
</verification>

<success_criteria>
- tests/test_seed_data.py criado com os 5 testes especificados
- Todos os testes falham (RED) com erro de import — connect_ai/seed_data.py ainda nao existe
- Os testes cobrem SEED-01, SEED-02, SEED-03, SEED-04 conforme mapeamento:
  - SEED-04: test_reproducibilidade_seed_fixa
  - SEED-03: test_perfil_teste_e_explicito
  - SEED-01: test_pool_tem_minimo_100_perfis + test_pool_diversidade
  - SEED-02: test_pool_tem_10_compativeis_com_perfil_teste
</success_criteria>

<output>
Apos a conclusao, criar `.planning/phases/02-seed-data-sintetico/02-01-SUMMARY.md`
</output>
