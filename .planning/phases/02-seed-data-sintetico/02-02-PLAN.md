---
phase: 02-seed-data-sintetico
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - connect_ai/seed_data.py
autonomous: true
requirements:
  - SEED-01
  - SEED-02
  - SEED-03
  - SEED-04

must_haves:
  truths:
    - "connect_ai/seed_data.py existe e e importavel sem erros"
    - "SEED_FIXA = 42 esta definida no modulo"
    - "PERFIL_TESTE e uma instancia explicita de Perfil com nome='Ana Lima', objetivo='namoro', idade=27 e >= 5 interesses fixos"
    - "gerar_pool_perfis(seed=42) retorna >= 100 instancias validas de Perfil"
    - "Duas chamadas gerar_pool_perfis(seed=42) retornam pools identicos (mesmo nome em cada posicao)"
    - "Analise estatica do pool confirma >= 10 perfis com interesses_em_comum >= 3, objetivo='namoro' e faixa_etaria_pref cobrindo idade 27"
    - "Pool cobre >= 5 cidades unicas, os 3 objetivos e idades de 18 a 60+"
    - "python -m pytest tests/test_seed_data.py retorna 5 passed, 0 failed"
  artifacts:
    - path: "connect_ai/seed_data.py"
      provides: "Gerador de perfis sinteticos com seed fixa"
      exports: ["SEED_FIXA", "PERFIL_TESTE", "gerar_pool_perfis"]
      contains: "SEED_FIXA = 42"
  key_links:
    - from: "connect_ai/seed_data.py"
      to: "connect_ai/schema.py"
      via: "from connect_ai.schema import Perfil"
      pattern: "from connect_ai.schema import"
    - from: "tests/test_seed_data.py"
      to: "connect_ai/seed_data.py"
      via: "from connect_ai.seed_data import PERFIL_TESTE, SEED_FIXA, gerar_pool_perfis"
      pattern: "from connect_ai.seed_data import"
---

<objective>
Implementar connect_ai/seed_data.py com SEED_FIXA, PERFIL_TESTE e gerar_pool_perfis() ate que todos os 5 testes do Plano 01 passem (GREEN).

Purpose: Pool calibrado matematicamente garante que a Fase 5 (gate critico) encontrara >= 10 matches com score >= 85 para o perfil de teste. Sem este calibre, o gate falha.

Output: connect_ai/seed_data.py com ~100+ perfis sinteticos reproduziveis, 15-20 dos quais sao de alta compatibilidade com PERFIL_TESTE (margem de seguranca para o threshold da Fase 5).
</objective>

<execution_context>
@C:/Users/power/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/power/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@connect_ai/schema.py
@tests/test_seed_data.py
@.planning/phases/02-seed-data-sintetico/02-01-SUMMARY.md
</context>

<interfaces>
<!-- Assinaturas de connect_ai/schema.py que seed_data.py deve importar e usar -->

```python
# connect_ai/schema.py

Genero = Literal["feminino", "masculino", "nao_binario", "outro"]
GeneroPreferido = Literal["feminino", "masculino", "nao_binario", "outro", "todos"]
Objetivo = Literal["namoro", "casual", "amizade"]

class Perfil(BaseModel):
    id: str = Field(default_factory=gerar_uuid)   # auto-gerado se omitido
    nome: str                                       # max 120 chars
    idade: int                                      # 18-99
    cidade: str
    genero: Genero
    genero_preferido: GeneroPreferido
    faixa_etaria_pref: Tuple[int, int]             # (min, max), ambos 18-99, min <= max
    objetivo: Objetivo
    bio: str                                        # min 1 char
    interesses: List[str]                           # minimo 1 elemento nao-vazio
    foto_perfil: Optional[str] = None
    fotos_extras: List[str] = Field(default_factory=list)
    personalidade_ia: Optional[str] = None
```

Criterio de compatibilidade usado nos testes (sem ChromaDB):
```python
# Um perfil p e "compativel" com PERFIL_TESTE se:
# 1. len(set(p.interesses) & set(PERFIL_TESTE.interesses)) >= 3
# 2. p.objetivo == PERFIL_TESTE.objetivo  # "namoro"
# 3. p.faixa_etaria_pref[0] <= PERFIL_TESTE.idade <= p.faixa_etaria_pref[1]  # cobre 27
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1: Implementar connect_ai/seed_data.py (fase GREEN)</name>
  <files>connect_ai/seed_data.py</files>
  <read_first>
    - tests/test_seed_data.py (criterios exatos que a implementacao deve satisfazer — ler ANTES de escrever uma linha de codigo)
    - connect_ai/schema.py (assinatura completa de Perfil, Genero, Objetivo, etc.)
    - connect_ai/__init__.py (para verificar se ha imports automaticos a considerar)
  </read_first>
  <behavior>
    - SEED_FIXA = 42 (constante inteira no topo do modulo)
    - PERFIL_TESTE: Perfil com nome="Ana Lima", idade=27, cidade="Sao Paulo", genero="feminino", genero_preferido="masculino", faixa_etaria_pref=(23, 35), objetivo="namoro", bio="Apaixonada por musica ao vivo, adoro explorar novos destinos e registrar tudo pela fotografia. Yoga faz parte da minha rotina e cinema e meu programa favorito nos fins de semana.", interesses=["musica", "viagem", "fotografia", "yoga", "cinema", "arte", "gastronomia"]
    - gerar_pool_perfis(seed=42) retorna lista de Perfil com len >= 100
    - Reproducibilidade: random.seed(seed) antes de qualquer sorteiro; nomes/dados sorteados de listas fixas definidas no modulo
    - Perfis de alta compatibilidade (>= 15 no pool): objetivo="namoro", faixa_etaria_pref cobre 27, interesses com >= 3 dos ["musica", "viagem", "fotografia", "yoga", "cinema", "arte", "gastronomia"]; distribuir entre generos feminino/masculino/nao_binario
    - Perfis de diversidade: cobrir cidades ["Sao Paulo", "Rio de Janeiro", "Belo Horizonte", "Curitiba", "Porto Alegre", "Salvador", "Fortaleza", "Recife"], objetivos ["namoro", "casual", "amizade"], idades 18-60
  </behavior>
  <action>
Criar connect_ai/seed_data.py com a seguinte estrutura:

**Secao 1 — Cabecalho e imports:**
```python
"""Gerador de perfis sinteticos calibrados para o CONNECT.AI.

Produz um pool reproduzivel de >= 100 perfis com seed fixa (SEED_FIXA = 42).
O pool e calibrado para garantir >= 10 perfis estatisticamente compativeis
com PERFIL_TESTE (analise estrutural: interesses, objetivo, faixa etaria),
o que e pre-condicao para o gate de score >= 85 da Fase 5.

Nao depende de ChromaDB, Gemini ou rede — todos os dados sao sinteticos e
definidos estaticamente neste modulo.
"""

from __future__ import annotations

import random
from typing import List

from connect_ai.schema import Perfil, gerar_uuid
```

**Secao 2 — Constantes de seed:**
```python
SEED_FIXA: int = 42
```

**Secao 3 — PERFIL_TESTE (campos TODOS explicitamente documentados):**
```python
PERFIL_TESTE = Perfil(
    id="perfil-teste-ana-lima-001",
    nome="Ana Lima",
    idade=27,
    cidade="Sao Paulo",
    genero="feminino",
    genero_preferido="masculino",
    faixa_etaria_pref=(23, 35),
    objetivo="namoro",
    bio=(
        "Apaixonada por musica ao vivo, adoro explorar novos destinos e registrar "
        "tudo pela fotografia. Yoga faz parte da minha rotina e cinema e meu "
        "programa favorito nos fins de semana."
    ),
    interesses=["musica", "viagem", "fotografia", "yoga", "cinema", "arte", "gastronomia"],
)
```

**Secao 4 — Listas de dados sinteticos (definidas como constantes de modulo):**
Definir as seguintes listas fixas (nao-aleatorias) usadas pelo gerador:
- _NOMES_FEMININOS: lista com >= 20 nomes femininos brasileiros comuns (ex: "Julia Santos", "Camila Rocha", "Fernanda Costa", "Beatriz Alves", ...)
- _NOMES_MASCULINOS: lista com >= 20 nomes masculinos (ex: "Lucas Ferreira", "Matheus Lima", "Gabriel Souza", "Rafael Oliveira", ...)
- _NOMES_NEUTROS: lista com >= 5 nomes de genero neutro (ex: "Alex Carvalho", "Jordan Pereira", "Sam Ribeiro", ...)
- _CIDADES: ["Sao Paulo", "Rio de Janeiro", "Belo Horizonte", "Curitiba", "Porto Alegre", "Salvador", "Fortaleza", "Recife", "Manaus", "Brasilia"]
- _TODOS_INTERESSES: lista com >= 30 interesses em PT-BR (incluir obrigatoriamente: "musica", "viagem", "fotografia", "yoga", "cinema", "arte", "gastronomia", "leitura", "esportes", "tecnologia", "culinaria", "danca", "teatro", "natureza", "meditacao", "games", "series", "academia", "ciclismo", "trilhas")
- _BIOS_ALTA_COMPATIBILIDADE: lista com >= 20 strings de bio curtas em PT-BR que mencionam musica, viagem ou fotografia (para perfis de alta compatibilidade)
- _BIOS_GERAIS: lista com >= 30 strings de bio curtas e variadas em PT-BR (para perfis gerais)

**Secao 5 — Funcao _gerar_perfil_alta_compatibilidade(rng, indice):**
Funcao interna que gera um perfil garantidamente compativel com PERFIL_TESTE:
- objetivo fixo = "namoro"
- faixa_etaria_pref que cobre idade 27: sortear (min, max) onde min <= 27 e max >= 27; ex: (rng.randint(18, 27), rng.randint(27, 40))
- interesses: sortear 4-5 interesses de _TODOS_INTERESSES MAIS incluir pelo menos 3 dos ["musica", "viagem", "fotografia", "yoga", "cinema"] para garantir intersecao >= 3 com PERFIL_TESTE
- genero: sortear entre ["feminino", "masculino", "nao_binario"] com pesos [0.5, 0.4, 0.1]
- genero_preferido: sortear entre ["feminino", "masculino", "todos"] com pesos [0.4, 0.4, 0.2]
- nome: sortear de _NOMES_FEMININOS se genero=="feminino", _NOMES_MASCULINOS se "masculino", _NOMES_NEUTROS se outro; adicionar sufixo com indice para evitar duplicatas
- cidade: sortear de _CIDADES
- idade: rng.randint(22, 36)
- bio: sortear de _BIOS_ALTA_COMPATIBILIDADE

**Secao 6 — Funcao _gerar_perfil_diverso(rng, indice):**
Funcao interna que gera perfil de diversidade (sem restricao de compatibilidade):
- objetivo: rng.choice(["namoro", "casual", "amizade"]) com pesos iguais
- faixa_etaria_pref: sorteio aleatorio valido entre 18-60
- interesses: sortear 3-6 de _TODOS_INTERESSES sem garantia de sobreposicao
- genero, genero_preferido, nome, cidade: sortear livremente
- idade: rng.randint(18, 60)
- bio: sortear de _BIOS_GERAIS

**Secao 7 — Funcao principal:**
```python
def gerar_pool_perfis(seed: int = SEED_FIXA) -> List[Perfil]:
    """Gera pool reproduzivel de perfis sinteticos.

    Retorna lista com:
      - 20 perfis de alta compatibilidade com PERFIL_TESTE (garantia para SEED-02)
      - 80 perfis de diversidade (cobre SEED-01)
    Total: 100 perfis.

    Args:
        seed: Semente para o gerador pseudo-aleatorio. Default SEED_FIXA=42.
              Usar seed diferente para testes de robustez.

    Returns:
        Lista de 100 instancias de Perfil validadas pelo Pydantic.
    """
    rng = random.Random(seed)   # Random local — nao altera o estado global
    perfis: List[Perfil] = []

    # 20 perfis de alta compatibilidade (garante SEED-02: >= 10 compativeis)
    for i in range(20):
        perfis.append(_gerar_perfil_alta_compatibilidade(rng, i))

    # 80 perfis de diversidade (garante SEED-01: diversidade sem vies obvio)
    for i in range(80):
        perfis.append(_gerar_perfil_diverso(rng, i + 20))

    # Embaralhar para que alta-compat nao fique sempre nos primeiros 20
    rng.shuffle(perfis)

    return perfis
```

ATENCAO — detalhes criticos:
1. Usar `random.Random(seed)` (instancia local), NUNCA `random.seed()` global — evita side-effects em testes paralelos
2. Cada perfil precisa de id unico: usar `gerar_uuid()` importado do schema OU gerar deterministicamente com f"seed-{indice:04d}"
3. Garantir que os 20 perfis de alta compatibilidade tenham PELO MENOS 3 interesses em comum com PERFIL_TESTE.interesses = ["musica", "viagem", "fotografia", "yoga", "cinema", "arte", "gastronomia"] — a forma mais segura e incluir 3 destes fixamente e sortear o resto
4. Toda string de bio deve ter pelo menos 10 caracteres (validacao do Pydantic exige min_length=1, mas bio vazia causaria ValidationError)
5. faixa_etaria_pref deve ser Tuple[int, int] com min <= max e ambos entre 18-99

Ciclo TDD esperado:
  1. Escrever o arquivo conforme especificado acima
  2. Rodar: python -m pytest tests/test_seed_data.py -v
  3. Se algum teste falhar, ler a mensagem de erro e corrigir APENAS o que falhou
  4. Repetir ate 5 passed, 0 failed
  </action>
  <verify>
    <automated>cd "C:/Users/power/OneDrive/Documentos/PROJETOS_/Trabalhos/Faculdade/CP5_PLN" && python -m pytest tests/test_seed_data.py -v 2>&1</automated>
  </verify>
  <done>
    - connect_ai/seed_data.py existe e e importavel: python -c "from connect_ai.seed_data import SEED_FIXA, PERFIL_TESTE, gerar_pool_perfis; print('OK')" imprime "OK"
    - SEED_FIXA = 42: grep "SEED_FIXA = 42" connect_ai/seed_data.py retorna 1 linha
    - PERFIL_TESTE.nome == "Ana Lima": verificavel no codigo com grep 'nome="Ana Lima"' connect_ai/seed_data.py
    - python -m pytest tests/test_seed_data.py retorna "5 passed, 0 failed"
    - python -m pytest tests/ retorna sem regressoes nos testes existentes (test_schema.py, test_config.py, test_scoring_utils.py, test_repositorio.py)
  </done>
  <acceptance_criteria>
    - grep "SEED_FIXA = 42" connect_ai/seed_data.py retorna 1 linha
    - grep 'nome="Ana Lima"' connect_ai/seed_data.py retorna 1 linha
    - grep 'objetivo="namoro"' connect_ai/seed_data.py retorna pelo menos 1 linha (PERFIL_TESTE)
    - grep "def gerar_pool_perfis(" connect_ai/seed_data.py retorna 1 linha
    - grep "random.Random(seed)" connect_ai/seed_data.py retorna 1 linha (instancia local, nao random.seed() global)
    - python -c "from connect_ai.seed_data import gerar_pool_perfis; p=gerar_pool_perfis(); assert len(p)>=100, f'Esperado >=100, obtido {len(p)}'; print('OK')" imprime "OK"
    - python -m pytest tests/test_seed_data.py -v 2>&1 | grep "5 passed" retorna 1 linha
    - python -m pytest tests/ 2>&1 | grep "failed" retorna vazio (sem regressoes)
  </acceptance_criteria>
</task>

</tasks>

<verification>
Verificacao final da Fase 2:

1. Reproducibilidade:
   python -c "from connect_ai.seed_data import gerar_pool_perfis; a=gerar_pool_perfis(42); b=gerar_pool_perfis(42); print('OK' if [p.nome for p in a]==[p.nome for p in b] else 'FALHOU')"

2. Pool >= 100 perfis:
   python -c "from connect_ai.seed_data import gerar_pool_perfis; p=gerar_pool_perfis(); assert len(p)>=100; print(f'Pool: {len(p)} perfis')"

3. PERFIL_TESTE explicito:
   python -c "from connect_ai.seed_data import PERFIL_TESTE; assert PERFIL_TESTE.nome=='Ana Lima'; assert PERFIL_TESTE.idade==27; print('PERFIL_TESTE OK')"

4. >= 10 compativeis (analise estatica):
   python -m pytest tests/test_seed_data.py::test_pool_tem_10_compativeis_com_perfil_teste -v

5. Suite completa:
   python -m pytest tests/test_seed_data.py -v

6. Sem regressoes:
   python -m pytest tests/ -v
</verification>

<success_criteria>
- connect_ai/seed_data.py existe com SEED_FIXA, PERFIL_TESTE e gerar_pool_perfis exportados
- python -m pytest tests/test_seed_data.py retorna "5 passed, 0 failed"
- python -m pytest tests/ retorna sem falhas (sem regressoes)
- Criterios de sucesso da Fase 2 todos verificaveis:
  1. Reproducibilidade com seed=42: teste test_reproducibilidade_seed_fixa passa
  2. Perfil de teste documentado (nao-aleatorio): PERFIL_TESTE com campos fixos
  3. >= 10 compativeis analise estatica: teste test_pool_tem_10_compativeis_com_perfil_teste passa
  4. Diversidade sem vies: teste test_pool_diversidade passa
</success_criteria>

<output>
Apos a conclusao, criar `.planning/phases/02-seed-data-sintetico/02-02-SUMMARY.md`
</output>
