---
phase: 04-pipeline-de-ingestao
plan: "02"
type: execute
wave: 2
depends_on:
  - "04-01"
files_modified:
  - connect_ai/ingestao.py
autonomous: true
requirements:
  - ING-01
  - ING-02
  - ING-03
  - ING-04

must_haves:
  truths:
    - "ingerir_perfil(perfil, colecao) chama agente_perfilador, gera embedding, persiste no ChromaDB e loga em PT-BR"
    - "ingerir_lote(perfis, colecao) processa lista completa e retorna {sucesso, falha, total}"
    - "Re-ingerir o mesmo perfil nao aumenta colecao.contar() (idempotencia via upsert do Repositorio)"
    - "Todos os 6 testes de test_ingestao.py passam (GREEN); suite completa sem regressoes"
    - "Embedding usa google.generativeai com model='models/text-embedding-004', task_type='RETRIEVAL_DOCUMENT'; mock quando GOOGLE_API_KEY ausente"
  artifacts:
    - path: "connect_ai/ingestao.py"
      provides: "Pipeline de ingestao de perfis"
      min_lines: 80
      exports:
        - "ingerir_perfil"
        - "ingerir_lote"
  key_links:
    - from: "connect_ai/ingestao.py"
      to: "connect_ai/agentes.agente_perfilador"
      via: "chamada direta agente_perfilador({'perfil': perfil, ...})"
      pattern: "agente_perfilador"
    - from: "connect_ai/ingestao.py"
      to: "connect_ai/schema.construir_documento_semantico"
      via: "chamada para montar texto do embedding"
      pattern: "construir_documento_semantico"
    - from: "connect_ai/ingestao.py"
      to: "connect_ai/repositorio.Repositorio.inserir"
      via: "colecao.inserir(perfil_processado, embedding)"
      pattern: "colecao\\.inserir"
---

<objective>
Fase GREEN do ciclo TDD: implementar connect_ai/ingestao.py ate todos os 6 testes passarem.

O modulo expoe duas funcoes publicas:
  - `ingerir_perfil(perfil, colecao)` — fluxo individual: valida → Perfilador → embedding → upsert
  - `ingerir_lote(perfis, colecao)` — itera `ingerir_perfil` e agrega contagens

Purpose: Fechar ING-01..ING-04 com implementacao minima que passa os testes RED do Plano 01.
Output: connect_ai/ingestao.py funcional; `pytest tests/test_ingestao.py` com 6 passed, 0 failed.
</objective>

<execution_context>
@C:/Users/power/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/power/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-pipeline-de-ingestao/04-01-SUMMARY.md

<interfaces>
<!-- Contratos ja implementados que ingestao.py DEVE usar. -->

De connect_ai/agentes.py:
```python
class AgentState(TypedDict):
    perfil: Perfil
    candidatos: List[Dict[str, Any]]
    matches: List[Dict[str, Any]]
    justificativas: Dict[str, str]
    erro: Optional[str]

def agente_perfilador(state: AgentState) -> AgentState:
    """Preenche state['perfil'].personalidade_ia via mock deterministico."""
    # Uso: state_saida = agente_perfilador({"perfil": perfil, "candidatos": [], ...})
    # Retorna dict com "perfil" tendo personalidade_ia preenchida.
```

De connect_ai/schema.py:
```python
def construir_documento_semantico(perfil: Perfil) -> str:
    """Retorna texto PT-BR: 'Bio: ... Interesses: ... Objetivo: ... Personalidade: ...'
    Omite secao Personalidade se personalidade_ia for None/vazio."""
```

De connect_ai/repositorio.py:
```python
class Repositorio:
    def inserir(self, perfil: Perfil, embedding: Sequence[float]) -> None:
        """Upsert idempotente — re-inserir mesmo id nao duplica."""
    def inserir_lote(self, perfis, embeddings) -> int: ...
    def contar(self) -> int: ...
    def existe(self, id_perfil: str) -> bool: ...
```

De connect_ai/config.py:
```python
def obter_chave_api(nome: str, padrao: Optional[str] = None) -> str:
    """Lanca ConfigError se ausente e padrao=None; retorna padrao se fornecido."""
```

API de embedding (google.generativeai):
```python
import google.generativeai as genai
genai.configure(api_key=GOOGLE_API_KEY)
resultado = genai.embed_content(
    model="models/text-embedding-004",
    content=texto,
    task_type="RETRIEVAL_DOCUMENT",
)
vetor = resultado["embedding"]  # List[float] com 768 dimensoes
```

Mock de embedding (quando GOOGLE_API_KEY ausente):
```python
# Vetor deterministico baseado em hash do texto (nao aleatorio)
import hashlib
hash_bytes = hashlib.md5(texto.encode()).digest()
vetor_mock = [(b / 255.0) for b in hash_bytes] * (768 // 16 + 1)
vetor_mock = vetor_mock[:768]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Tarefa 1 GREEN: Implementar connect_ai/ingestao.py</name>
  <files>connect_ai/ingestao.py</files>

  <read_first>
    - tests/test_ingestao.py (OBRIGATORIO — ler os 6 testes antes de escrever uma linha)
    - connect_ai/agentes.py (assinatura de agente_perfilador e AgentState)
    - connect_ai/schema.py (Perfil, construir_documento_semantico)
    - connect_ai/repositorio.py (Repositorio.inserir, contar, existe)
    - connect_ai/config.py (obter_chave_api — para ler GOOGLE_API_KEY com fallback)
  </read_first>

  <behavior>
    - test_ingerir_perfil_retorna_dict: ingerir_perfil retorna dict com chaves "sucesso", "id", "erro"
    - test_ingerir_perfil_persiste_no_chromadb: colecao.contar() == 1 apos ingerir_perfil
    - test_ingerir_lote_retorna_dict_com_contagens: resultado["total"] == 5 para 5 perfis
    - test_ingerir_lote_todos_perfis_inseridos: colecao.contar() == 10 para 10 perfis
    - test_ingerir_perfil_emite_log_ptbr: caplog contem "ingeri", "ingerindo" ou "perfil"
    - test_idempotencia_nao_duplica: colecao.contar() == 1 apos 2x ingerir_perfil mesmo perfil
  </behavior>

  <action>
    Criar connect_ai/ingestao.py com o seguinte conteudo:

    1. DOCSTRING de modulo em PT-BR descrevendo o pipeline de ingestao (ING-01..ING-04).

    2. Imports:
       ```python
       from __future__ import annotations
       import hashlib
       import logging
       import os
       from typing import List
       import google.generativeai as genai
       from connect_ai.agentes import AgentState, agente_perfilador
       from connect_ai.config import obter_chave_api
       from connect_ai.repositorio import Repositorio
       from connect_ai.schema import Perfil, construir_documento_semantico
       ```

    3. Logger de modulo (PT-BR em mensagens):
       ```python
       logger = logging.getLogger(__name__)
       ```

    4. Funcao privada `_gerar_embedding(texto: str) -> list[float]`:
       - Tenta ler GOOGLE_API_KEY via `obter_chave_api("GOOGLE_API_KEY", padrao="")`
       - Se api_key vazio: retorna embedding mock deterministico via hashlib.md5:
         ```python
         hash_bytes = hashlib.md5(texto.encode("utf-8")).digest()  # 16 bytes
         vetor = [(b / 255.0) for b in hash_bytes] * 48 + [(b / 255.0) for b in hash_bytes[:16]]
         return vetor[:768]  # exatamente 768 dimensoes
         ```
       - Se api_key presente: chama `genai.configure(api_key=api_key)` e
         `genai.embed_content(model="models/text-embedding-004", content=texto, task_type="RETRIEVAL_DOCUMENT")`
         e retorna `resultado["embedding"]`
       - Nao loga a api_key; loga apenas "Gerando embedding via API Google" ou "Gerando embedding via mock"

    5. Funcao publica `ingerir_perfil(perfil: Perfil, colecao: Repositorio) -> dict`:
       Fluxo:
       a) Logar inicio: `logger.info("Ingerindo perfil id=%s nome=%s", perfil.id, perfil.nome)`
       b) Chamar agente_perfilador para preencher personalidade_ia:
          ```python
          state_inicial: AgentState = {
              "perfil": perfil,
              "candidatos": [],
              "matches": [],
              "justificativas": {},
              "erro": None,
          }
          state_saida = agente_perfilador(state_inicial)
          perfil_processado = state_saida["perfil"]
          ```
       c) Construir documento semantico: `texto = construir_documento_semantico(perfil_processado)`
       d) Gerar embedding: `embedding = _gerar_embedding(texto)`
       e) Persistir: `colecao.inserir(perfil_processado, embedding)`
          (upsert idempotente — o Repositorio ja garante ING-04)
       f) Logar conclusao: `logger.info("Perfil id=%s ingerido com sucesso.", perfil_processado.id)`
       g) Retornar: `{"sucesso": True, "id": perfil_processado.id, "erro": None}`

       Tratar excecoes com bloco try/except Exception:
       - Logar: `logger.error("Falha ao ingerir perfil id=%s: %s", perfil.id, str(e))`
       - Retornar: `{"sucesso": False, "id": perfil.id, "erro": str(e)}`

    6. Funcao publica `ingerir_lote(perfis: List[Perfil], colecao: Repositorio) -> dict`:
       Fluxo:
       a) Logar inicio: `logger.info("Iniciando ingestao de lote com %d perfis.", len(perfis))`
       b) Iterar sobre perfis chamando `ingerir_perfil` individualmente:
          ```python
          sucesso = 0
          falha = 0
          for perfil in perfis:
              resultado = ingerir_perfil(perfil, colecao)
              if resultado["sucesso"]:
                  sucesso += 1
              else:
                  falha += 1
          ```
       c) Logar resultado: `logger.info("Lote concluido: %d sucesso, %d falha, %d total.", sucesso, falha, len(perfis))`
       d) Retornar: `{"sucesso": sucesso, "falha": falha, "total": len(perfis)}`

    RESTRICOES CRITICAS:
    - Nao usar `print()` — apenas `logging` (ING-03)
    - Todas as mensagens de log em PT-BR
    - Nao chamar `genai.configure()` fora de `_gerar_embedding` (efeito colateral global)
    - O modulo deve ser importavel sem GOOGLE_API_KEY (fallback para mock)
    - Nao modificar connect_ai/repositorio.py — a idempotencia vem do upsert ja implementado
  </action>

  <verify>
    <automated>cd "C:\Users\power\OneDrive\Documentos\PROJETOS_\Trabalhos\Faculdade\CP5_PLN" && python -m pytest tests/test_ingestao.py -v 2>&1</automated>
  </verify>

  <done>
    - connect_ai/ingestao.py existe com >= 80 linhas
    - `python -m pytest tests/test_ingestao.py -v` mostra: 6 passed, 0 failed
    - `python -m pytest tests/ -v` mostra suite completa passando sem regressoes (todos os testes anteriores mantidos)
    - `python -c "from connect_ai.ingestao import ingerir_perfil, ingerir_lote; print('OK')"` retorna OK sem GOOGLE_API_KEY
    - Grep confirma logs PT-BR: `grep -n "logger\." connect_ai/ingestao.py` mostra mensagens contendo "Ingerindo", "ingerido", "Iniciando", "concluido"
    - Grep confirma ausencia de print: `grep -n "print(" connect_ai/ingestao.py` retorna vazio
  </done>
</task>

<task type="auto">
  <name>Tarefa 2: Verificar suite completa e confirmar criterios de fase</name>
  <files>connect_ai/ingestao.py</files>

  <read_first>
    - connect_ai/ingestao.py (verificar o arquivo recem-criado)
    - tests/test_ingestao.py (confirmar que testes batem com implementacao)
  </read_first>

  <action>
    Executar as seguintes verificacoes na ordem e corrigir qualquer falha em connect_ai/ingestao.py:

    1. Suite completa sem regressao:
       `python -m pytest tests/ -v --tb=short`
       Resultado esperado: todos os testes passam (incluindo test_config, test_schema,
       test_scoring_utils, test_repositorio, test_seed_data, test_agentes, test_grafo, test_ingestao).

    2. Idempotencia de lote (criterio de sucesso da Fase 4, item 2):
       ```python
       from connect_ai.repositorio import Repositorio
       from connect_ai.seed_data import gerar_pool_perfis
       from connect_ai.ingestao import ingerir_lote
       import tempfile, os

       with tempfile.TemporaryDirectory() as tmp:
           colecao = Repositorio(diretorio=tmp, nome_colecao="teste_idempotencia")
           perfis = gerar_pool_perfis()[:5]
           ingerir_lote(perfis, colecao)
           contagem_1 = colecao.contar()
           ingerir_lote(perfis, colecao)  # re-ingerir mesmo lote
           contagem_2 = colecao.contar()
           assert contagem_1 == contagem_2, f"Idempotencia falhou: {contagem_1} -> {contagem_2}"
           print(f"Idempotencia OK: {contagem_1} documentos antes e depois da re-ingestao")
       ```
       Salvar como script temporario e executar: `python /tmp/check_idempotencia.py`
       (ou embutir no terminal Python direto)

    3. Banco nao vazio apos lote (criterio de sucesso da Fase 4, item 3):
       Usando o mesmo script acima, apos ingerir_lote(perfis_seed, colecao) verificar
       que colecao.contar() > 0 e que colecao.buscar(embedding_dummy, n_resultados=1)
       retorna pelo menos 1 resultado.
       Embedding dummy: [0.1] * 768

    Se qualquer verificacao falhar, corrigir connect_ai/ingestao.py para que passe.
    Nao modificar os testes — os testes sao a especificacao.
  </action>

  <verify>
    <automated>cd "C:\Users\power\OneDrive\Documentos\PROJETOS_\Trabalhos\Faculdade\CP5_PLN" && python -m pytest tests/ -v --tb=short 2>&1 | tail -30</automated>
  </verify>

  <done>
    - `python -m pytest tests/ -v` mostra 0 failed (suite completa verde)
    - Idempotencia confirmada: contagem antes == contagem depois da re-ingestao
    - Banco nao vazio: colecao.contar() > 0 apos ingerir_lote com seed data parcial
  </done>
</task>

</tasks>

<verification>
Verificacoes finais da Fase 4:

1. Modulo importavel sem credenciais:
   python -c "from connect_ai.ingestao import ingerir_perfil, ingerir_lote; print('OK')"

2. Testes RED passando GREEN:
   python -m pytest tests/test_ingestao.py -v
   Esperado: 6 passed

3. Suite completa sem regressao:
   python -m pytest tests/ -v
   Esperado: todos passando

4. Logs PT-BR presentes (ING-03):
   grep -n "logger\." connect_ai/ingestao.py
   Esperado: linhas com "Ingerindo", "ingerido", "Iniciando", "concluido", "Falha"

5. Sem credenciais hardcoded (ING-03, ACC-09):
   grep -n "AIza\|api_key\s*=" connect_ai/ingestao.py
   Esperado: nenhum resultado
</verification>

<success_criteria>
- connect_ai/ingestao.py existe e exporta ingerir_perfil e ingerir_lote
- 6/6 testes de test_ingestao.py passam
- Suite completa (todos os arquivos em tests/) passa sem regressao
- ingerir_lote(perfis_seed) insere perfis e logs mostram contagem correta (ING-01, ING-02, ING-03)
- Re-ingestao do mesmo perfil nao aumenta contar() (ING-04)
- Modulo funciona sem GOOGLE_API_KEY (mock de embedding via hashlib)
</success_criteria>

<output>
Apos conclusao, criar .planning/phases/04-pipeline-de-ingestao/04-02-SUMMARY.md com:
- Estrutura de connect_ai/ingestao.py (funcoes, linhas aproximadas)
- Resultado final do pytest (N passed)
- Decisoes de implementacao (mock de embedding, logger name, etc.)
- Confirmacao dos 3 criterios de sucesso da Fase 4
</output>
