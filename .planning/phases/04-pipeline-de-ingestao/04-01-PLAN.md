---
phase: 04-pipeline-de-ingestao
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/test_ingestao.py
autonomous: true
requirements:
  - ING-01
  - ING-02
  - ING-03
  - ING-04

must_haves:
  truths:
    - "tests/test_ingestao.py existe com pelo menos 6 testes que FALHAM antes da implementacao"
    - "Os testes cobrem ingerir_perfil, ingerir_lote, idempotencia e logs PT-BR"
    - "Nenhum teste em test_ingestao.py passa (RED puro) — connect_ai/ingestao.py ainda nao existe"
  artifacts:
    - path: "tests/test_ingestao.py"
      provides: "Suite RED cobrindo ING-01..ING-04"
      min_lines: 80
      exports: []
  key_links:
    - from: "tests/test_ingestao.py"
      to: "connect_ai/ingestao.py"
      via: "import direto"
      pattern: "from connect_ai.ingestao import"
---

<objective>
Fase RED do ciclo TDD para o Pipeline de Ingestao.

Escrever tests/test_ingestao.py com todos os testes falhando (ImportError/AssertionError)
antes de connect_ai/ingestao.py existir. Os testes definem o contrato publico das funcoes
`ingerir_perfil` e `ingerir_lote` e provam que o modulo ainda nao existe.

Purpose: Garantir que a implementacao (Plano 02) seja guiada pelos testes e nao o contrario.
Output: tests/test_ingestao.py com 6+ testes, todos com status FAILED no pytest.
</objective>

<execution_context>
@C:/Users/power/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/power/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Contratos que os testes irão importar (modulo ainda nao existe — gera ImportError RED). -->

De connect_ai/schema.py (ja implementado):
```python
class Perfil(BaseModel):
    id: str  # default_factory=gerar_uuid
    nome: str
    idade: int  # ge=18, le=99
    cidade: str
    genero: Genero          # Literal["feminino","masculino","nao_binario","outro"]
    genero_preferido: GeneroPreferido  # + "todos"
    faixa_etaria_pref: Tuple[int, int]
    objetivo: Objetivo      # Literal["namoro","casual","amizade"]
    bio: str
    interesses: List[str]
    foto_perfil: Optional[str] = None
    fotos_extras: List[str] = []
    personalidade_ia: Optional[str] = None

def construir_documento_semantico(perfil: Perfil) -> str: ...
```

De connect_ai/seed_data.py (ja implementado):
```python
PERFIL_TESTE: Perfil  # id="perfil-teste-ana-lima-001", nome="Ana Lima", ...
def gerar_pool_perfis() -> List[Perfil]: ...  # retorna 100 perfis
```

De connect_ai/repositorio.py (ja implementado):
```python
class Repositorio:
    def inserir(self, perfil: Perfil, embedding: Sequence[float]) -> None: ...
    def inserir_lote(self, perfis, embeddings) -> int: ...
    def contar(self) -> int: ...
    def existe(self, id_perfil: str) -> bool: ...
    def resetar(self) -> None: ...
```

Contrato do modulo A SER CRIADO (connect_ai/ingestao.py):
```python
def ingerir_perfil(perfil: Perfil, colecao: Repositorio) -> dict:
    """Retorna {"sucesso": bool, "id": str, "erro": str | None}"""

def ingerir_lote(perfis: List[Perfil], colecao: Repositorio) -> dict:
    """Retorna {"sucesso": int, "falha": int, "total": int}"""
```
</interfaces>
</context>

<tasks>

<task type="tdd">
  <name>Tarefa 1 RED: Escrever tests/test_ingestao.py com todos os testes falhando</name>
  <files>tests/test_ingestao.py</files>

  <read_first>
    - tests/conftest.py (padrao de fixture e sys.path — replicar exatamente)
    - tests/test_agentes.py (padrao de fixtures com PERFIL_TESTE, estrutura dos testes)
    - connect_ai/schema.py (campos de Perfil — para montar perfis de teste validos)
    - connect_ai/seed_data.py (PERFIL_TESTE e gerar_pool_perfis — reusar)
    - connect_ai/repositorio.py (Repositorio.contar, existe, resetar — para fixtures)
  </read_first>

  <behavior>
    Os testes cobrem exatamente os 4 requisitos ING-01..ING-04:

    ING-01 — ingerir_perfil executa fluxo completo:
    - test_ingerir_perfil_retorna_dict: ingerir_perfil(PERFIL_TESTE, colecao) retorna dict com chaves "sucesso", "id", "erro"
    - test_ingerir_perfil_persiste_no_chromadb: apos ingerir_perfil, colecao.contar() == 1

    ING-02 — ingerir_lote processa lista:
    - test_ingerir_lote_retorna_dict_com_contagens: ingerir_lote(perfis[:5], colecao) retorna dict com chaves "sucesso", "falha", "total"
    - test_ingerir_lote_todos_perfis_inseridos: ingerir_lote(perfis_seed[:10], colecao); colecao.contar() == 10

    ING-03 — logs em PT-BR (verificado via caplog):
    - test_ingerir_perfil_emite_log_ptbr: apos ingerir_perfil, caplog contém ao menos uma mensagem com string "Ingerindo" ou "Ingeri" ou "perfil" (usando pytest caplog)

    ING-04 — idempotencia:
    - test_idempotencia_nao_duplica: ingerir_perfil duas vezes o mesmo perfil; colecao.contar() permanece 1 (nao cresce para 2)
  </behavior>

  <action>
    Criar tests/test_ingestao.py com o seguinte conteudo EXATO (nao resumir, nao omitir):

    1. DOCSTRING de modulo explicando que sao testes RED, citando ING-01..ING-04.

    2. Imports (todos dentro das funcoes de teste via import lazy, conforme padrao 03-01):
       - `from connect_ai.ingestao import ingerir_perfil, ingerir_lote` DENTRO de cada teste
         (garante que cada teste falha individualmente com ImportError em RED)

    3. Fixture `colecao_temporaria`:
       - Usa `tmp_path` do pytest para criar Repositorio com diretorio temporario isolado
       - Instancia: `Repositorio(diretorio=str(tmp_path / "chroma_test"), nome_colecao="test_ingestao")`
       - Chama `colecao.resetar()` no teardown via yield
       - Importa Repositorio no corpo da fixture (nao no topo do modulo)

    4. Fixture `perfil_unico`:
       - Retorna um Perfil valido com campos fixos (nao aleatorios):
         id="test-ing-001", nome="Teste Ingestao", idade=25, cidade="Sao Paulo",
         genero="masculino", genero_preferido="feminino", faixa_etaria_pref=(22,32),
         objetivo="namoro", bio="Perfil de teste para ingestao unitaria.",
         interesses=["musica", "viagem", "cinema"]

    5. Seis testes com nomes EXATOS:
       a) test_ingerir_perfil_retorna_dict(colecao_temporaria, perfil_unico)
          - Importa ingerir_perfil lazily dentro do corpo
          - resultado = ingerir_perfil(perfil_unico, colecao_temporaria)
          - assert isinstance(resultado, dict)
          - assert "sucesso" in resultado
          - assert "id" in resultado
          - assert "erro" in resultado

       b) test_ingerir_perfil_persiste_no_chromadb(colecao_temporaria, perfil_unico)
          - Importa ingerir_perfil lazily
          - ingerir_perfil(perfil_unico, colecao_temporaria)
          - assert colecao_temporaria.contar() == 1

       c) test_ingerir_lote_retorna_dict_com_contagens(colecao_temporaria)
          - Importa ingerir_lote lazily; importa gerar_pool_perfis de connect_ai.seed_data
          - perfis = gerar_pool_perfis()[:5]
          - resultado = ingerir_lote(perfis, colecao_temporaria)
          - assert isinstance(resultado, dict)
          - assert "sucesso" in resultado and "falha" in resultado and "total" in resultado
          - assert resultado["total"] == 5

       d) test_ingerir_lote_todos_perfis_inseridos(colecao_temporaria)
          - Importa ingerir_lote lazily; importa gerar_pool_perfis
          - perfis = gerar_pool_perfis()[:10]
          - ingerir_lote(perfis, colecao_temporaria)
          - assert colecao_temporaria.contar() == 10

       e) test_ingerir_perfil_emite_log_ptbr(colecao_temporaria, perfil_unico, caplog)
          - import logging; importa ingerir_perfil lazily
          - with caplog.at_level(logging.INFO):
              ingerir_perfil(perfil_unico, colecao_temporaria)
          - mensagens = " ".join(r.message for r in caplog.records)
          - assert any(palavra in mensagens.lower() for palavra in ["ingeri", "ingerindo", "perfil"])

       f) test_idempotencia_nao_duplica(colecao_temporaria, perfil_unico)
          - Importa ingerir_perfil lazily
          - ingerir_perfil(perfil_unico, colecao_temporaria)
          - ingerir_perfil(perfil_unico, colecao_temporaria)  # segunda chamada
          - assert colecao_temporaria.contar() == 1  # nao cresceu para 2

    IMPORTANTE: Nao criar connect_ai/ingestao.py nesta tarefa. Os 6 testes DEVEM falhar com
    ImportError ("cannot import name 'ingerir_perfil' from 'connect_ai.ingestao'" ou
    ModuleNotFoundError). Isso confirma o RED.
  </action>

  <verify>
    <automated>cd "C:\Users\power\OneDrive\Documentos\PROJETOS_\Trabalhos\Faculdade\CP5_PLN" && python -m pytest tests/test_ingestao.py -v 2>&1 | tail -20</automated>
  </verify>

  <done>
    - tests/test_ingestao.py existe com exatamente 6 testes
    - `python -m pytest tests/test_ingestao.py -v` mostra 6 FAILED (ModuleNotFoundError ou ImportError)
    - Nenhum teste passa (0 passed)
    - connect_ai/ingestao.py NAO existe no sistema de arquivos
  </done>
</task>

</tasks>

<verification>
Executar apos a tarefa:
  python -m pytest tests/test_ingestao.py -v

Resultado esperado: 6 FAILED, 0 passed, com erros do tipo:
  ModuleNotFoundError: No module named 'connect_ai.ingestao'
  -- ou --
  ImportError: cannot import name 'ingerir_perfil' from 'connect_ai.ingestao'

Verificar que connect_ai/ingestao.py NAO existe:
  python -c "import connect_ai.ingestao" deve falhar
</verification>

<success_criteria>
- tests/test_ingestao.py existe e tem >= 80 linhas
- 6 testes identificados por pytest (collected 6 items)
- 0 testes passam, 6 falham (RED confirmado)
- connect_ai/ingestao.py nao existe (ModuleNotFoundError e o sinal de RED correto)
</success_criteria>

<output>
Apos conclusao, criar .planning/phases/04-pipeline-de-ingestao/04-01-SUMMARY.md com:
- Testes criados e seus nomes exatos
- Confirmacao do estado RED (6 FAILED)
- Qualquer decisao de design tomada na estrutura dos testes
</output>
